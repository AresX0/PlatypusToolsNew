<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- 
    KLINGON EMPIRE Theme (tlhIngan wo')
    Inspired by the warrior culture of Qo'noS
    
    Color Palette:
    - Blood Red backgrounds (#5C0000, #8B0000)
    - Metallic Gold text and accents (#FFD700, #D4A574)
    - White text for high contrast (#FFFFFF, #F0F0F0)
    
    Qapla'! (Success!)
    -->

    <!-- ======================== Klingon Font Family ======================== -->
    <!-- Uses system-installed 'klingon font' with fallbacks. Install via Settings > Advanced > Custom Fonts -->
    <FontFamily x:Key="KlingonFontFamily">klingon font, Consolas, Segoe UI, Arial</FontFamily>
    <FontFamily x:Key="KlingonDisplayFont">klingon font, Consolas, Segoe UI, Arial</FontFamily>

    <!-- ======================== Klingon Color Palette ======================== -->
    
    <!-- Primary Colors -->
    <Color x:Key="KlingonBloodRed">#FF8B0000</Color>
    <Color x:Key="KlingonDarkRed">#FF5C0000</Color>
    <Color x:Key="KlingonDeepRed">#FF3A0000</Color>
    <Color x:Key="KlingonCrimson">#FFDC143C</Color>
    
    <!-- Metallic Colors -->
    <Color x:Key="KlingonGold">#FFD4A574</Color>
    <Color x:Key="KlingonGoldBright">#FFFFD700</Color>
    <Color x:Key="KlingonCopper">#FFB87333</Color>
    <Color x:Key="KlingonBronze">#FF8B4513</Color>
    <Color x:Key="KlingonSteel">#FF4A4A4A</Color>
    
    <!-- Dark Colors -->
    <Color x:Key="KlingonBlack">#FF0A0000</Color>
    <Color x:Key="KlingonCharcoal">#FF1A0808</Color>
    
    <!-- UI Colors - HIGH CONTRAST TEXT -->
    <Color x:Key="KlingonText">#FFFFFFFF</Color>
    <Color x:Key="KlingonTextBright">#FFFFD700</Color>
    <Color x:Key="KlingonTextDim">#FFD4A574</Color>
    
    <!-- Window Background/Foreground -->
    <Color x:Key="WindowBackgroundColor">#FF1A0505</Color>
    <Color x:Key="WindowForegroundColor">#FFFFFFFF</Color>
    <Color x:Key="ControlBackgroundColor">#FF2A0808</Color>
    <Color x:Key="ControlBorderColor">#FF8B0000</Color>

    <!-- ======================== Klingon Brushes ======================== -->
    
    <!-- Primary Brushes -->
    <SolidColorBrush x:Key="KlingonBloodRedBrush" Color="{StaticResource KlingonBloodRed}"/>
    <SolidColorBrush x:Key="KlingonDarkRedBrush" Color="{StaticResource KlingonDarkRed}"/>
    <SolidColorBrush x:Key="KlingonDeepRedBrush" Color="{StaticResource KlingonDeepRed}"/>
    <SolidColorBrush x:Key="KlingonCrimsonBrush" Color="{StaticResource KlingonCrimson}"/>
    <SolidColorBrush x:Key="KlingonGoldBrush" Color="{StaticResource KlingonGold}"/>
    <SolidColorBrush x:Key="KlingonGoldBrightBrush" Color="{StaticResource KlingonGoldBright}"/>
    <SolidColorBrush x:Key="KlingonCharcoalBrush" Color="{StaticResource KlingonCharcoal}"/>
    <SolidColorBrush x:Key="KlingonTextBrush" Color="{StaticResource KlingonText}"/>
    <SolidColorBrush x:Key="KlingonTextBrightBrush" Color="{StaticResource KlingonTextBright}"/>
    <SolidColorBrush x:Key="KlingonTextDimBrush" Color="{StaticResource KlingonTextDim}"/>

    <!-- ======================== Theme Resource Mappings ======================== -->
    
    <SolidColorBrush x:Key="WindowBackgroundBrush" Color="{StaticResource WindowBackgroundColor}"/>
    <SolidColorBrush x:Key="WindowForegroundBrush" Color="{StaticResource WindowForegroundColor}"/>
    <SolidColorBrush x:Key="ControlBackgroundBrush" Color="{StaticResource ControlBackgroundColor}"/>
    <SolidColorBrush x:Key="ControlForegroundBrush" Color="{StaticResource KlingonText}"/>
    <SolidColorBrush x:Key="ControlBorderBrush" Color="{StaticResource ControlBorderColor}"/>
    <SolidColorBrush x:Key="MenuForegroundBrush" Color="{StaticResource WindowForegroundColor}"/>
    
    <!-- Standard theme keys -->
    <SolidColorBrush x:Key="TextPrimary" Color="{StaticResource KlingonText}"/>
    <SolidColorBrush x:Key="TextSecondary" Color="{StaticResource KlingonTextDim}"/>
    <SolidColorBrush x:Key="TextPrimaryBrush" Color="{StaticResource KlingonText}"/>
    <SolidColorBrush x:Key="TextSecondaryBrush" Color="{StaticResource KlingonTextDim}"/>
    <SolidColorBrush x:Key="SecondaryTextBrush" Color="{StaticResource KlingonTextDim}"/>
    <SolidColorBrush x:Key="ForegroundDimBrush" Color="{StaticResource KlingonTextDim}"/>
    <SolidColorBrush x:Key="BackgroundBrush" Color="{StaticResource WindowBackgroundColor}"/>
    <SolidColorBrush x:Key="SurfaceBrush" Color="{StaticResource KlingonCharcoal}"/>
    <SolidColorBrush x:Key="CardBackground" Color="#FF2A0808"/>
    <SolidColorBrush x:Key="CardBackgroundBrush" Color="#FF2A0808"/>
    <SolidColorBrush x:Key="SurfaceVariantBrush" Color="#FF3A1010"/>
    <SolidColorBrush x:Key="ForegroundBrush" Color="#FFFFFFFF"/>
    <SolidColorBrush x:Key="BorderBrush" Color="{StaticResource KlingonBloodRed}"/>
    <SolidColorBrush x:Key="AccentBrush" Color="{StaticResource KlingonGoldBright}"/>
    <SolidColorBrush x:Key="AccentHoverBrush" Color="{StaticResource KlingonGold}"/>
    <SolidColorBrush x:Key="PrimaryBrush" Color="{StaticResource KlingonBloodRed}"/>
    <SolidColorBrush x:Key="PrimaryHoverBrush" Color="{StaticResource KlingonCrimson}"/>
    <SolidColorBrush x:Key="SelectionBackgroundBrush" Color="{StaticResource KlingonBloodRed}"/>
    <SolidColorBrush x:Key="DisabledForegroundBrush" Color="#FF808080"/>
    
    <!-- Title Bar -->
    <SolidColorBrush x:Key="TitleBarBackgroundBrush" Color="{StaticResource KlingonDarkRed}"/>
    <SolidColorBrush x:Key="TitleBarForegroundBrush" Color="{StaticResource KlingonText}"/>
    <SolidColorBrush x:Key="TitleBarButtonHoverBrush" Color="#33FFD700"/>
    <SolidColorBrush x:Key="TitleBarCloseHoverBrush" Color="#FFFF0000"/>
    <SolidColorBrush x:Key="HeaderBrush" Color="{StaticResource KlingonDarkRed}"/>
    <SolidColorBrush x:Key="HeaderForegroundBrush" Color="{StaticResource KlingonText}"/>
    
    <!-- Tab styling -->
    <SolidColorBrush x:Key="TabHeaderBackground" Color="{StaticResource KlingonDarkRed}"/>
    <SolidColorBrush x:Key="TabHeaderForeground" Color="{StaticResource KlingonText}"/>
    <SolidColorBrush x:Key="TabSelectedBackground" Color="{StaticResource KlingonBloodRed}"/>
    <SolidColorBrush x:Key="TabSelectedForeground" Color="{StaticResource KlingonTextBright}"/>
    <SolidColorBrush x:Key="TabHoverBackground" Color="{StaticResource KlingonCrimson}"/>
    
    <!-- Status brushes -->
    <SolidColorBrush x:Key="SuccessBrush" Color="#FF8B4513"/>
    <SolidColorBrush x:Key="WarningBrush" Color="#FFFF4500"/>
    <SolidColorBrush x:Key="ErrorBrush" Color="#FFFF0000"/>
    <SolidColorBrush x:Key="InfoBrush" Color="{StaticResource KlingonCopper}"/>
    
    <!-- Warning/Info box brushes -->
    <SolidColorBrush x:Key="WarningBackgroundBrush" Color="#FF3D2010"/>
    <SolidColorBrush x:Key="WarningBorderBrush" Color="#FFFF9800"/>
    <SolidColorBrush x:Key="WarningForegroundBrush" Color="#FFFFCC80"/>
    <SolidColorBrush x:Key="InfoBackgroundBrush" Color="#FF2A1515"/>
    <SolidColorBrush x:Key="InfoBorderBrush" Color="{StaticResource KlingonGold}"/>
    <SolidColorBrush x:Key="InfoForegroundBrush" Color="{StaticResource KlingonGold}"/>
    <SolidColorBrush x:Key="SuccessBackgroundBrush" Color="#FF1E2A1E"/>
    <SolidColorBrush x:Key="SuccessBorderBrush" Color="#FF8B4513"/>
    <SolidColorBrush x:Key="SuccessForegroundBrush" Color="#FFD4A574"/>
    <SolidColorBrush x:Key="ErrorBackgroundBrush" Color="#FF3D1010"/>
    <SolidColorBrush x:Key="ErrorBorderBrush" Color="#FFFF0000"/>
    <SolidColorBrush x:Key="ErrorForegroundBrush" Color="#FFFF8080"/>

    <!-- ======================== Gradient Brushes ======================== -->
    
    <LinearGradientBrush x:Key="KlingonHeaderGradient" StartPoint="0,0" EndPoint="1,0">
        <GradientStop Color="{StaticResource KlingonDarkRed}" Offset="0"/>
        <GradientStop Color="{StaticResource KlingonBloodRed}" Offset="0.5"/>
        <GradientStop Color="{StaticResource KlingonDarkRed}" Offset="1"/>
    </LinearGradientBrush>
    
    <LinearGradientBrush x:Key="KlingonButtonGradient" StartPoint="0,0" EndPoint="0,1">
        <GradientStop Color="{StaticResource KlingonCrimson}" Offset="0"/>
        <GradientStop Color="{StaticResource KlingonBloodRed}" Offset="0.5"/>
        <GradientStop Color="{StaticResource KlingonDarkRed}" Offset="1"/>
    </LinearGradientBrush>

    <!-- ======================== DEFAULT STYLES (No x:Key = applies to all) ======================== -->
    
    <!-- Window -->
    <Style TargetType="Window">
        <Setter Property="Background" Value="{StaticResource WindowBackgroundBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource WindowForegroundBrush}"/>
    </Style>
    
    <!-- Grid -->
    <Style TargetType="Grid">
        <Setter Property="Background" Value="Transparent"/>
    </Style>
    
    <!-- DockPanel -->
    <Style TargetType="DockPanel">
        <Setter Property="Background" Value="{StaticResource WindowBackgroundBrush}"/>
    </Style>

    <!-- TextBlock - WHITE text with Klingon font for visibility -->
    <Style TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource WindowForegroundBrush}"/>
        <Setter Property="FontFamily" Value="{StaticResource KlingonFontFamily}"/>
    </Style>

    <!-- Label - WHITE text with Klingon font for visibility -->
    <Style TargetType="Label">
        <Setter Property="Foreground" Value="{StaticResource WindowForegroundBrush}"/>
        <Setter Property="FontFamily" Value="{StaticResource KlingonFontFamily}"/>
    </Style>

    <!-- TextBox -->
    <Style TargetType="TextBox">
        <Setter Property="Background" Value="{StaticResource ControlBackgroundBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource WindowForegroundBrush}"/>
        <Setter Property="FontFamily" Value="{StaticResource KlingonFontFamily}"/>
        <Setter Property="BorderBrush" Value="{StaticResource KlingonBloodRedBrush}"/>
        <Setter Property="CaretBrush" Value="{StaticResource KlingonGoldBrightBrush}"/>
        <Setter Property="SelectionBrush" Value="{StaticResource KlingonBloodRedBrush}"/>
        <Setter Property="BorderThickness" Value="2"/>
        <Setter Property="Padding" Value="6,4"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TextBox">
                    <Border x:Name="border"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="0">
                        <ScrollViewer x:Name="PART_ContentHost" 
                                      Margin="{TemplateBinding Padding}"
                                      Focusable="False"
                                      HorizontalScrollBarVisibility="Hidden"
                                      VerticalScrollBarVisibility="Hidden"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.5"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource KlingonCrimsonBrush}"/>
                        </Trigger>
                        <Trigger Property="IsFocused" Value="True">
                            <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource KlingonGoldBrightBrush}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- PasswordBox -->
    <Style TargetType="PasswordBox">
        <Setter Property="Background" Value="{StaticResource ControlBackgroundBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource WindowForegroundBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource KlingonBloodRedBrush}"/>
        <Setter Property="CaretBrush" Value="{StaticResource KlingonGoldBrightBrush}"/>
        <Setter Property="SelectionBrush" Value="{StaticResource KlingonBloodRedBrush}"/>
        <Setter Property="BorderThickness" Value="2"/>
        <Setter Property="Padding" Value="6,4"/>
    </Style>

    <!-- Button -->
    <Style TargetType="Button">
        <Setter Property="Background" Value="{StaticResource KlingonButtonGradient}"/>
        <Setter Property="Foreground" Value="{StaticResource WindowForegroundBrush}"/>
        <Setter Property="FontFamily" Value="{StaticResource KlingonFontFamily}"/>
        <Setter Property="BorderBrush" Value="{StaticResource KlingonGoldBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="8,4"/>
        <Setter Property="FontWeight" Value="Bold"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="MinWidth" Value="0"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="border" 
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="0"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="Background" Value="{StaticResource KlingonCrimsonBrush}"/>
                            <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource KlingonGoldBrightBrush}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="border" Property="Background" Value="{StaticResource KlingonDarkRedBrush}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.5"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- CheckBox -->
    <Style TargetType="CheckBox">
        <Setter Property="Foreground" Value="{StaticResource WindowForegroundBrush}"/>
        <Setter Property="FontFamily" Value="{StaticResource KlingonFontFamily}"/>
        <Setter Property="BorderBrush" Value="{StaticResource KlingonBloodRedBrush}"/>
        <Setter Property="Background" Value="{StaticResource ControlBackgroundBrush}"/>
    </Style>

    <!-- RadioButton -->
    <Style TargetType="RadioButton">
        <Setter Property="Foreground" Value="{StaticResource WindowForegroundBrush}"/>
        <Setter Property="FontFamily" Value="{StaticResource KlingonFontFamily}"/>
        <Setter Property="BorderBrush" Value="{StaticResource KlingonBloodRedBrush}"/>
        <Setter Property="Background" Value="{StaticResource ControlBackgroundBrush}"/>
    </Style>

    <!-- ComboBox Toggle Button Template -->
    <ControlTemplate x:Key="KlingonComboBoxToggleButton" TargetType="ToggleButton">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition Width="20" />
            </Grid.ColumnDefinitions>
            <Border x:Name="Border" 
                    Grid.ColumnSpan="2"
                    Background="{StaticResource ControlBackgroundBrush}"
                    BorderBrush="{StaticResource KlingonBloodRedBrush}"
                    BorderThickness="2"
                    CornerRadius="0" />
            <Border Grid.Column="0"
                    Margin="2"
                    Background="{StaticResource ControlBackgroundBrush}" 
                    BorderThickness="0" />
            <Path x:Name="Arrow"
                  Grid.Column="1"     
                  Fill="{StaticResource KlingonGoldBrightBrush}"
                  HorizontalAlignment="Center"
                  VerticalAlignment="Center"
                  Data="M0,0 L4,4 L8,0 Z" />
        </Grid>
        <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="Border" Property="BorderBrush" Value="{StaticResource KlingonGoldBrush}" />
            </Trigger>
            <Trigger Property="IsChecked" Value="True">
                <Setter TargetName="Border" Property="BorderBrush" Value="{StaticResource KlingonGoldBrightBrush}" />
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <ControlTemplate x:Key="KlingonComboBoxTextBox" TargetType="TextBox">
        <Border x:Name="PART_ContentHost" Focusable="False" Background="{TemplateBinding Background}" />
    </ControlTemplate>

    <!-- ComboBox -->
    <Style TargetType="ComboBox">
        <Setter Property="Background" Value="{StaticResource ControlBackgroundBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource WindowForegroundBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource KlingonBloodRedBrush}"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="OverridesDefaultStyle" Value="True"/>
        <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Auto"/>
        <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto"/>
        <Setter Property="ScrollViewer.CanContentScroll" Value="True"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ComboBox">
                    <Grid>
                        <ToggleButton x:Name="ToggleButton" 
                                      Template="{StaticResource KlingonComboBoxToggleButton}"
                                      Focusable="False"
                                      IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                      ClickMode="Press" />
                        <ContentPresenter x:Name="ContentSite"
                                          IsHitTestVisible="False" 
                                          Content="{TemplateBinding SelectionBoxItem}"
                                          ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                          ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                          Margin="6,3,23,3"
                                          VerticalAlignment="Center"
                                          HorizontalAlignment="Left">
                            <ContentPresenter.Resources>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Foreground" Value="{StaticResource WindowForegroundBrush}" />
                                </Style>
                            </ContentPresenter.Resources>
                        </ContentPresenter>
                        <TextBox x:Name="PART_EditableTextBox"
                                 Style="{x:Null}" 
                                 Template="{StaticResource KlingonComboBoxTextBox}" 
                                 HorizontalAlignment="Left" 
                                 VerticalAlignment="Center" 
                                 Margin="3,3,23,3"
                                 Focusable="True" 
                                 Background="Transparent"
                                 Foreground="{StaticResource WindowForegroundBrush}"
                                 CaretBrush="{StaticResource KlingonGoldBrightBrush}"
                                 Visibility="Hidden"
                                 IsReadOnly="{TemplateBinding IsReadOnly}" />
                        <Popup x:Name="Popup"
                               Placement="Bottom"
                               IsOpen="{TemplateBinding IsDropDownOpen}"
                               AllowsTransparency="True" 
                               Focusable="False"
                               PopupAnimation="Slide">
                            <Grid x:Name="DropDown"
                                  SnapsToDevicePixels="True"                
                                  MinWidth="{TemplateBinding ActualWidth}"
                                  MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                <Border x:Name="DropDownBorder"
                                        Background="{StaticResource ControlBackgroundBrush}"
                                        BorderThickness="2"
                                        BorderBrush="{StaticResource KlingonGoldBrush}"/>
                                <ScrollViewer Margin="4,6,4,6" SnapsToDevicePixels="True">
                                    <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained" />
                                </ScrollViewer>
                            </Grid>
                        </Popup>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="HasItems" Value="False">
                            <Setter TargetName="DropDownBorder" Property="MinHeight" Value="95" />
                        </Trigger>
                        <Trigger Property="IsEditable" Value="True">
                            <Setter TargetName="PART_EditableTextBox" Property="Visibility" Value="Visible" />
                            <Setter TargetName="ContentSite" Property="Visibility" Value="Hidden" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    
    <!-- ComboBoxItem -->
    <Style TargetType="ComboBoxItem">
        <Setter Property="Background" Value="{StaticResource ControlBackgroundBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource WindowForegroundBrush}"/>
        <Setter Property="Padding" Value="6,4"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ComboBoxItem">
                    <Border x:Name="Border"
                            Padding="{TemplateBinding Padding}"
                            Background="{TemplateBinding Background}"
                            SnapsToDevicePixels="True">
                        <ContentPresenter>
                            <ContentPresenter.Resources>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Foreground" Value="{StaticResource WindowForegroundBrush}" />
                                </Style>
                            </ContentPresenter.Resources>
                        </ContentPresenter>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsHighlighted" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="{StaticResource KlingonBloodRedBrush}" />
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="{StaticResource KlingonDarkRedBrush}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ListBox -->
    <Style TargetType="ListBox">
        <Setter Property="Background" Value="{StaticResource ControlBackgroundBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource WindowForegroundBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource KlingonBloodRedBrush}"/>
        <Setter Property="BorderThickness" Value="2"/>
    </Style>

    <!-- ListBoxItem -->
    <Style TargetType="ListBoxItem">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="{StaticResource WindowForegroundBrush}"/>
        <Setter Property="Padding" Value="8,6"/>
        <Setter Property="BorderThickness" Value="0,0,0,1"/>
        <Setter Property="BorderBrush" Value="{StaticResource KlingonDarkRedBrush}"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{StaticResource KlingonDarkRedBrush}"/>
            </Trigger>
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Background" Value="{StaticResource KlingonBloodRedBrush}"/>
                <Setter Property="Foreground" Value="{StaticResource KlingonTextBrightBrush}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- ListView -->
    <Style TargetType="ListView">
        <Setter Property="Background" Value="{StaticResource ControlBackgroundBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource WindowForegroundBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource KlingonBloodRedBrush}"/>
        <Setter Property="BorderThickness" Value="2"/>
    </Style>

    <!-- ListViewItem -->
    <Style TargetType="ListViewItem">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="{StaticResource WindowForegroundBrush}"/>
        <Setter Property="Padding" Value="8,6"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{StaticResource KlingonDarkRedBrush}"/>
            </Trigger>
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Background" Value="{StaticResource KlingonBloodRedBrush}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- DataGrid -->
    <Style TargetType="DataGrid">
        <Setter Property="Background" Value="{StaticResource WindowBackgroundBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource WindowForegroundBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource KlingonBloodRedBrush}"/>
        <Setter Property="BorderThickness" Value="2"/>
        <Setter Property="GridLinesVisibility" Value="Horizontal"/>
        <Setter Property="HorizontalGridLinesBrush" Value="{StaticResource KlingonDarkRedBrush}"/>
        <Setter Property="VerticalGridLinesBrush" Value="{StaticResource KlingonDarkRedBrush}"/>
        <Setter Property="RowBackground" Value="Transparent"/>
        <Setter Property="AlternatingRowBackground" Value="#FF200808"/>
    </Style>

    <!-- DataGridColumnHeader -->
    <Style TargetType="DataGridColumnHeader">
        <Setter Property="Background" Value="{StaticResource KlingonHeaderGradient}"/>
        <Setter Property="Foreground" Value="{StaticResource WindowForegroundBrush}"/>
        <Setter Property="Padding" Value="10,8"/>
        <Setter Property="FontWeight" Value="Bold"/>
        <Setter Property="BorderBrush" Value="{StaticResource KlingonGoldBrush}"/>
        <Setter Property="BorderThickness" Value="0,0,1,2"/>
    </Style>

    <!-- DataGridRow -->
    <Style TargetType="DataGridRow">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="{StaticResource WindowForegroundBrush}"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{StaticResource KlingonDarkRedBrush}"/>
            </Trigger>
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Background" Value="{StaticResource KlingonBloodRedBrush}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- DataGridCell -->
    <Style TargetType="DataGridCell">
        <Setter Property="Foreground" Value="{StaticResource WindowForegroundBrush}"/>
        <Setter Property="BorderBrush" Value="Transparent"/>
        <Setter Property="Padding" Value="4,2"/>
        <Style.Triggers>
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Background" Value="{StaticResource KlingonBloodRedBrush}"/>
                <Setter Property="Foreground" Value="{StaticResource WindowForegroundBrush}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- GroupBox -->
    <Style TargetType="GroupBox">
        <Setter Property="BorderBrush" Value="{StaticResource KlingonBloodRedBrush}"/>
        <Setter Property="BorderThickness" Value="2"/>
        <Setter Property="Foreground" Value="{StaticResource KlingonGoldBrightBrush}"/>
        <Setter Property="Padding" Value="8"/>
        <Setter Property="Background" Value="{StaticResource ControlBackgroundBrush}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="GroupBox">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <Border Background="{StaticResource KlingonHeaderGradient}" 
                                BorderBrush="{StaticResource KlingonGoldBrush}" 
                                BorderThickness="2,2,2,0"
                                Padding="12,6">
                            <ContentPresenter ContentSource="Header" 
                                              TextElement.Foreground="{StaticResource WindowForegroundBrush}"
                                              TextElement.FontWeight="Bold"/>
                        </Border>
                        <Border Grid.Row="1" 
                                Background="{StaticResource ControlBackgroundBrush}"
                                BorderBrush="{StaticResource KlingonGoldBrush}"
                                BorderThickness="2,0,2,2"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter/>
                        </Border>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- TabControl -->
    <Style TargetType="TabControl">
        <Setter Property="Background" Value="{StaticResource WindowBackgroundBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource KlingonBloodRedBrush}"/>
    </Style>

    <!-- TabItem -->
    <Style TargetType="TabItem">
        <Setter Property="Background" Value="{StaticResource KlingonDarkRedBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource WindowForegroundBrush}"/>
        <Setter Property="Padding" Value="16,8"/>
        <Setter Property="BorderThickness" Value="2,2,2,0"/>
        <Setter Property="BorderBrush" Value="{StaticResource KlingonBloodRedBrush}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TabItem">
                    <Border x:Name="border"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            Padding="{TemplateBinding Padding}"
                            Margin="2,0,0,0">
                        <ContentPresenter x:Name="contentPresenter" 
                                          ContentSource="Header" 
                                          HorizontalAlignment="Center" 
                                          VerticalAlignment="Center"
                                          TextElement.Foreground="{StaticResource WindowForegroundBrush}"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="border" Property="Background" Value="{StaticResource KlingonBloodRedBrush}"/>
                            <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource KlingonGoldBrush}"/>
                            <Setter TargetName="contentPresenter" Property="TextElement.Foreground" Value="{StaticResource KlingonGoldBrightBrush}"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="Background" Value="{StaticResource KlingonCrimsonBrush}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Menu -->
    <Style TargetType="Menu">
        <Setter Property="Background" Value="{StaticResource KlingonDarkRedBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource WindowForegroundBrush}"/>
        <Setter Property="FontFamily" Value="{StaticResource KlingonFontFamily}"/>
    </Style>

    <!-- MenuItem - Full template with dark popup background for readability -->
    <Style TargetType="MenuItem">
        <Setter Property="Background" Value="{StaticResource KlingonDarkRedBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource WindowForegroundBrush}"/>
        <Setter Property="FontFamily" Value="{StaticResource KlingonFontFamily}"/>
        <Setter Property="Padding" Value="12,6"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="MenuItem">
                    <Border x:Name="Border" 
                            Background="{TemplateBinding Background}" 
                            BorderBrush="{StaticResource KlingonGoldBrush}" 
                            BorderThickness="0"
                            Padding="{TemplateBinding Padding}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" SharedSizeGroup="Icon"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto" SharedSizeGroup="Shortcut"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <ContentPresenter x:Name="Icon" Grid.Column="0" Margin="0,0,6,0" 
                                              VerticalAlignment="Center" ContentSource="Icon"/>
                            <TextBlock x:Name="HeaderText" Grid.Column="1" 
                                       Text="{TemplateBinding Header}"
                                       VerticalAlignment="Center"
                                       Foreground="{StaticResource WindowForegroundBrush}"/>
                            <TextBlock x:Name="InputGestureText" Grid.Column="2" 
                                       Text="{TemplateBinding InputGestureText}" 
                                       Margin="24,0,0,0" VerticalAlignment="Center"
                                       Foreground="{StaticResource KlingonGoldBrush}" Opacity="0.8"/>
                            <Path x:Name="Arrow" Grid.Column="3" 
                                  Data="M0,0 L4,4 L0,8 Z" 
                                  Fill="{StaticResource KlingonGoldBrush}"
                                  VerticalAlignment="Center" Margin="6,0,0,0"
                                  Visibility="Collapsed"/>
                            <Popup x:Name="PART_Popup" 
                                   IsOpen="{Binding IsSubmenuOpen, RelativeSource={RelativeSource TemplatedParent}}"
                                   Placement="Bottom" 
                                   AllowsTransparency="True" 
                                   Focusable="False"
                                   PopupAnimation="Fade">
                                <Border Background="{StaticResource KlingonDarkRedBrush}" 
                                        BorderBrush="{StaticResource KlingonGoldBrush}" 
                                        BorderThickness="1">
                                    <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Cycle"/>
                                </Border>
                            </Popup>
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="Role" Value="TopLevelHeader">
                            <Setter TargetName="Arrow" Property="Visibility" Value="Collapsed"/>
                            <Setter TargetName="PART_Popup" Property="Placement" Value="Bottom"/>
                        </Trigger>
                        <Trigger Property="Role" Value="TopLevelItem">
                            <Setter TargetName="Arrow" Property="Visibility" Value="Collapsed"/>
                        </Trigger>
                        <Trigger Property="Role" Value="SubmenuHeader">
                            <Setter TargetName="Arrow" Property="Visibility" Value="Visible"/>
                            <Setter TargetName="PART_Popup" Property="Placement" Value="Right"/>
                        </Trigger>
                        <Trigger Property="Role" Value="SubmenuItem">
                            <Setter TargetName="Arrow" Property="Visibility" Value="Collapsed"/>
                        </Trigger>
                        <Trigger Property="IsHighlighted" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="{StaticResource KlingonBloodRedBrush}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="HeaderText" Property="Foreground" Value="{StaticResource KlingonGoldBrush}"/>
                            <Setter TargetName="HeaderText" Property="Opacity" Value="0.5"/>
                        </Trigger>
                        <Trigger Property="Icon" Value="{x:Null}">
                            <Setter TargetName="Icon" Property="Visibility" Value="Collapsed"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ProgressBar -->
    <Style TargetType="ProgressBar">
        <Setter Property="Background" Value="{StaticResource ControlBackgroundBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource KlingonBloodRedBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource KlingonGoldBrush}"/>
        <Setter Property="BorderThickness" Value="2"/>
        <Setter Property="Height" Value="20"/>
    </Style>

    <!-- Slider -->
    <Style TargetType="Slider">
        <Setter Property="Background" Value="{StaticResource ControlBackgroundBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource KlingonBloodRedBrush}"/>
    </Style>

    <!-- ToolTip -->
    <Style TargetType="ToolTip">
        <Setter Property="Background" Value="{StaticResource ControlBackgroundBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource WindowForegroundBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource KlingonGoldBrush}"/>
        <Setter Property="BorderThickness" Value="2"/>
        <Setter Property="Padding" Value="8,4"/>
    </Style>

    <!-- ScrollViewer -->
    <Style TargetType="ScrollViewer">
        <Setter Property="Background" Value="Transparent"/>
    </Style>

    <!-- Expander -->
    <Style TargetType="Expander">
        <Setter Property="Background" Value="{StaticResource ControlBackgroundBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource WindowForegroundBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource KlingonBloodRedBrush}"/>
    </Style>

    <!-- TreeView -->
    <Style TargetType="TreeView">
        <Setter Property="Background" Value="{StaticResource ControlBackgroundBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource WindowForegroundBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource KlingonBloodRedBrush}"/>
    </Style>

    <!-- TreeViewItem -->
    <Style TargetType="TreeViewItem">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="{StaticResource WindowForegroundBrush}"/>
        <Style.Triggers>
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Background" Value="{StaticResource KlingonBloodRedBrush}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- StatusBar -->
    <Style TargetType="StatusBar">
        <Setter Property="Background" Value="{StaticResource KlingonDarkRedBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource WindowForegroundBrush}"/>
    </Style>

    <!-- ToolBar -->
    <Style TargetType="ToolBar">
        <Setter Property="Background" Value="{StaticResource KlingonDarkRedBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource WindowForegroundBrush}"/>
    </Style>

    <!-- Separator -->
    <Style TargetType="Separator">
        <Setter Property="Background" Value="{StaticResource KlingonBloodRedBrush}"/>
    </Style>

    <!-- ContextMenu -->
    <Style TargetType="ContextMenu">
        <Setter Property="Background" Value="{StaticResource KlingonDarkRedBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource WindowForegroundBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource KlingonBloodRedBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
    </Style>

    <!-- ToggleButton -->
    <Style TargetType="ToggleButton">
        <Setter Property="Background" Value="{StaticResource KlingonDarkRedBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource WindowForegroundBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource KlingonBloodRedBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="8,4"/>
        <Style.Triggers>
            <Trigger Property="IsChecked" Value="True">
                <Setter Property="Background" Value="{StaticResource KlingonBloodRedBrush}"/>
                <Setter Property="Foreground" Value="{StaticResource KlingonGoldBrightBrush}"/>
            </Trigger>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{StaticResource KlingonCrimsonBrush}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- GridViewColumnHeader -->
    <Style TargetType="GridViewColumnHeader">
        <Setter Property="Background" Value="{StaticResource KlingonDarkRedBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource WindowForegroundBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource KlingonBloodRedBrush}"/>
        <Setter Property="BorderThickness" Value="0,0,1,1"/>
        <Setter Property="Padding" Value="6,3"/>
    </Style>

</ResourceDictionary>
