<UserControl x:Class="PlatypusTools.UI.Views.HiderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:PlatypusTools.UI.ViewModels"
             mc:Ignorable="d" d:DesignHeight="500" d:DesignWidth="800">
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,8">
            <TextBlock VerticalAlignment="Center" Margin="0,0,8,0">Folder:</TextBlock>
            <TextBox Width="300" Text="{Binding NewFolderPath, UpdateSourceTrigger=PropertyChanged}" />
            <Button Content="Browse..." Command="{Binding BrowseFolderCommand}" Width="80" Margin="8,0,0,0" />
            <Button Content="Add to Hider List" Command="{Binding AddFolderCommand}" Width="120" Margin="8,0,0,0" />
        </StackPanel>

        <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="0,0,0,8">
            <Button Content="Hide Folder" Command="{Binding SetHiddenCommand}" Width="100" />
            <Button Content="Unhide Folder" Command="{Binding ClearHiddenCommand}" Width="110" Margin="8,0,0,0" />
            <Button Content="Restrict to Administrators" Command="{Binding ApplyAclCommand}" Width="170" Margin="8,0,0,0" />
            <Button Content="Load Configuration" Command="{Binding LoadConfigCommand}" Width="130" Margin="8,0,0,0" />
            <Button Content="Save Configuration" Command="{Binding SaveConfigCommand}" Width="130" Margin="8,0,0,0" />
        </StackPanel>

        <ScrollViewer Grid.Row="2" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
            <DataGrid ItemsSource="{Binding Records}" AutoGenerateColumns="False"
                      CanUserResizeColumns="True" CanUserSortColumns="True"
                     HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Disabled"
                     ColumnWidth="*">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Folder" Binding="{Binding FolderPath}" Width="300" MinWidth="200" CanUserResize="True" IsReadOnly="True" />
                    <DataGridCheckBoxColumn Header="Hidden" Binding="{Binding IsHidden, Mode=OneWay}" Width="80" MinWidth="60" CanUserResize="True" IsReadOnly="True" />
                    <DataGridCheckBoxColumn Header="Restrict to Administrators" Binding="{Binding AclRestricted, UpdateSourceTrigger=PropertyChanged}" Width="160" MinWidth="140" CanUserResize="True" />
                    <DataGridCheckBoxColumn Header="Use EFS" Binding="{Binding EfsEnabled, UpdateSourceTrigger=PropertyChanged}" Width="100" MinWidth="80" CanUserResize="True" />
                    <DataGridTemplateColumn Header="Actions" Width="280" MinWidth="280">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="Hide" Command="{Binding DataContext.SetHiddenCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" Width="45" Margin="0,0,2,0" ToolTip="Hide this folder" />
                                <Button Content="Unhide" Command="{Binding DataContext.ClearHiddenCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" Width="50" Margin="0,0,2,0" ToolTip="Unhide this folder" />
                                <Button Content="ACL" Command="{Binding DataContext.ApplyAclCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" Width="40" Margin="0,0,2,0" ToolTip="Restrict to Administrators" />
                                <Button Content="EFS" Command="{Binding DataContext.ApplyEfsCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" Width="40" Margin="0,0,2,0" ToolTip="Apply EFS encryption" />
                                <Button Content="Remove" Command="{Binding DataContext.RemoveRecordCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" Width="55" Foreground="Red" ToolTip="Remove from list" />
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
        </ScrollViewer>
    </Grid>
</UserControl>