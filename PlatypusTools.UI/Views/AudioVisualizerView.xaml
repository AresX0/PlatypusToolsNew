<UserControl x:Class="PlatypusTools.UI.Views.AudioVisualizerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:skia="clr-namespace:SkiaSharp.Views.WPF;assembly=SkiaSharp.Views.WPF"
             mc:Ignorable="d"
             d:DesignHeight="300" 
             d:DesignWidth="600"
             Background="{DynamicResource WindowBackgroundBrush}">

    <Grid>
        <!-- Standard WPF Canvas for legacy modes -->
        <Canvas x:Name="VisualizerCanvas" Background="{DynamicResource WindowBackgroundBrush}" MouseLeftButtonDown="VisualizerCanvas_MouseLeftButtonDown"/>
        <!-- GPU-accelerated SkiaSharp surface for HD modes -->
        <skia:SKElement x:Name="SkiaCanvas" Visibility="Collapsed" PaintSurface="OnSkiaPaintSurface" MouseLeftButtonDown="VisualizerCanvas_MouseLeftButtonDown" />
    </Grid>

</UserControl>
