<UserControl x:Class="PlatypusTools.UI.Views.FileIntegrityView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignHeight="700" d:DesignWidth="1200">
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="5" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Text="🛡️ File Integrity Monitor" FontSize="16" FontWeight="Bold" Margin="0,0,0,8" />

        <!-- Controls -->
        <GroupBox Grid.Row="1" Header="Baseline Settings" Margin="0,0,0,4">
            <WrapPanel Margin="8">
                <TextBlock Text="Directory:" VerticalAlignment="Center" Margin="0,0,4,0" />
                <TextBox Text="{Binding DirectoryPath, UpdateSourceTrigger=PropertyChanged}" Width="300" Margin="0,0,4,0" />
                <Button Content="📁 Browse" Command="{Binding BrowseFolderCommand}" Padding="8,3" Margin="0,0,12,0" />
                <TextBlock Text="Algorithm:" VerticalAlignment="Center" Margin="0,0,4,0" />
                <ComboBox ItemsSource="{Binding Algorithms}" SelectedItem="{Binding SelectedAlgorithm}" Width="90" Margin="0,0,12,0" />
                <CheckBox Content="Recursive" IsChecked="{Binding Recursive}" VerticalAlignment="Center" Margin="0,0,16,0" />
                <Button Content="📷 Create Baseline" Command="{Binding CreateBaselineCommand}" Padding="12,4" Margin="0,0,4,0"
                        Background="#FF4CAF50" Foreground="White" />
                <Button Content="🔍 Compare" Command="{Binding CompareCommand}" Padding="12,4" Margin="0,0,4,0"
                        Background="#FF2196F3" Foreground="White" />
                <Button Content="💾 Save" Command="{Binding SaveBaselineCommand}" Padding="8,4" Margin="0,0,4,0"
                        Background="#FF9C27B0" Foreground="White" />
                <Button Content="📂 Load" Command="{Binding LoadBaselineCommand}" Padding="8,4" Margin="0,0,4,0"
                        Background="#FFFF9800" Foreground="White" />
                <Button Content="⛔ Cancel" Command="{Binding CancelCommand}" Padding="8,4"
                        Background="#FFF44336" Foreground="White" />
            </WrapPanel>
        </GroupBox>

        <GridSplitter Grid.Row="2" Height="5" HorizontalAlignment="Stretch"
                      Background="#CCCCCC" Cursor="SizeNS" ResizeDirection="Rows"/>

        <!-- Results -->
        <Grid Grid.Row="3">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!-- Summary Bar -->
            <StackPanel Orientation="Horizontal" Margin="0,4,0,4">
                <TextBlock Text="{Binding BaselineInfo}" FontWeight="SemiBold" VerticalAlignment="Center" Margin="0,0,20,0" />
                <Border Background="#FF4CAF50" CornerRadius="3" Padding="8,2" Margin="0,0,8,0">
                    <TextBlock Foreground="White">
                        <Run Text="Added: " /><Run Text="{Binding AddedCount, Mode=OneWay}" />
                    </TextBlock>
                </Border>
                <Border Background="#FFFF9800" CornerRadius="3" Padding="8,2" Margin="0,0,8,0">
                    <TextBlock Foreground="White">
                        <Run Text="Modified: " /><Run Text="{Binding ModifiedCount, Mode=OneWay}" />
                    </TextBlock>
                </Border>
                <Border Background="#FFF44336" CornerRadius="3" Padding="8,2">
                    <TextBlock Foreground="White">
                        <Run Text="Deleted: " /><Run Text="{Binding DeletedCount, Mode=OneWay}" />
                    </TextBlock>
                </Border>
            </StackPanel>

            <!-- Changes Grid -->
            <DataGrid Grid.Row="1" ItemsSource="{Binding Changes}"
                      AutoGenerateColumns="False" IsReadOnly="True" SelectionMode="Single"
                      GridLinesVisibility="Horizontal" AlternatingRowBackground="#FF2A2A3A"
                      Background="#FF1E1E2E" Foreground="White" BorderThickness="0">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="" Binding="{Binding ChangeIcon}" Width="35" />
                    <DataGridTextColumn Header="Change" Binding="{Binding ChangeType}" Width="70" />
                    <DataGridTextColumn Header="File" Binding="{Binding RelativePath}" Width="*" MinWidth="200">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="TextTrimming" Value="CharacterEllipsis" />
                                <Setter Property="ToolTip" Value="{Binding RelativePath}" />
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                    <DataGridTextColumn Header="Original Hash" Binding="{Binding OriginalHash}" Width="150">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="TextTrimming" Value="CharacterEllipsis" />
                                <Setter Property="ToolTip" Value="{Binding OriginalHash}" />
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                    <DataGridTextColumn Header="Current Hash" Binding="{Binding CurrentHash}" Width="150">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="TextTrimming" Value="CharacterEllipsis" />
                                <Setter Property="ToolTip" Value="{Binding CurrentHash}" />
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>

        <!-- Status Bar -->
        <Grid Grid.Row="4" Margin="0,4,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock Text="{Binding StatusMessage}" Foreground="#FFB0B0B0" FontSize="11" VerticalAlignment="Center" />
            <ProgressBar Grid.Column="1" Value="{Binding Progress}" Width="200" Height="14"
                         Visibility="{Binding IsScanning, Converter={StaticResource BoolToVisibilityConverter}}" />
        </Grid>
    </Grid>
</UserControl>
