<UserControl x:Class="PlatypusTools.UI.Views.ProcessManagerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignHeight="600" d:DesignWidth="1000">
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" MinHeight="60" />
            <RowDefinition Height="5" />
            <RowDefinition Height="*" MinHeight="100"/>
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Filter and Actions -->
        <GroupBox Header="Process Manager" Grid.Row="0" Margin="0,0,0,5">
            <Grid Margin="8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel Orientation="Horizontal" Grid.Column="0">
                    <TextBlock VerticalAlignment="Center" Width="80">Filter:</TextBlock>
                    <TextBox x:Name="FilterTextBox" Width="300" Text="{Binding FilterText, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,16,0" />
                    <Button Content="Refresh" Click="OnRefreshClick" MinWidth="180" Margin="0,0,8,0" />
                    <Button Content="Kill Process" Click="OnKillProcessClick" MinWidth="180" Background="DarkRed" Foreground="White" FontWeight="Bold" />
                </StackPanel>
                <TextBlock Grid.Column="1" VerticalAlignment="Center" FontWeight="Bold">
                    Total Processes: <Run x:Name="TotalProcessesText" Text="{Binding TotalProcesses, Mode=OneWay}" />
                </TextBlock>
            </Grid>
        </GroupBox>
        
        <!-- GridSplitter for resizing -->
        <GridSplitter Grid.Row="1" Height="5" HorizontalAlignment="Stretch" 
                      Background="#CCCCCC" Cursor="SizeNS" ResizeDirection="Rows"/>

        <!-- Process List -->
        <GroupBox Header="Running Processes" Grid.Row="2" Margin="0,5,0,8">
                <DataGrid x:Name="ProcessesDataGrid" ItemsSource="{Binding Processes}"
                          SelectedItem="{Binding SelectedProcess}"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False"
                          CanUserDeleteRows="False"
                          CanUserResizeColumns="True"
                          CanUserSortColumns="True"
                          IsReadOnly="True"
                          SelectionMode="Extended"
                          ColumnHeaderHeight="32"
                          MinHeight="250"
                          ColumnWidth="*"
                          ScrollViewer.HorizontalScrollBarVisibility="Auto" 
                          ScrollViewer.VerticalScrollBarVisibility="Auto"
                          ScrollViewer.CanContentScroll="True"
                          VirtualizingStackPanel.IsVirtualizing="True"
                          VirtualizingStackPanel.VirtualizationMode="Recycling"
                          EnableRowVirtualization="True"
                          SelectionChanged="ProcessesDataGrid_SelectionChanged">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="PID" Binding="{Binding ProcessId}" Width="80" MinWidth="60" IsReadOnly="True" CanUserResize="True"/>
                        <DataGridTextColumn Header="Process Name" Binding="{Binding ProcessName}" Width="200" MinWidth="150" IsReadOnly="True" CanUserResize="True"/>
                        <DataGridTextColumn Header="Memory (MB)" Binding="{Binding MemoryUsageDisplay}" Width="100" MinWidth="80" IsReadOnly="True" CanUserResize="True"/>
                        <DataGridTextColumn Header="CPU %" Binding="{Binding CPUUsage, StringFormat='{}{0:0.00}'}" Width="80" MinWidth="60" IsReadOnly="True" CanUserResize="True"/>
                        <DataGridTextColumn Header="Threads" Binding="{Binding ThreadCount}" Width="80" MinWidth="60" IsReadOnly="True" CanUserResize="True"/>
                        <DataGridTextColumn Header="User" Binding="{Binding UserName}" Width="120" MinWidth="100" IsReadOnly="True" CanUserResize="True"/>
                        <DataGridTextColumn Header="Start Time" Binding="{Binding StartTime, StringFormat='yyyy-MM-dd HH:mm:ss'}" Width="150" MinWidth="130" IsReadOnly="True" CanUserResize="True"/>
                        <DataGridTextColumn Header="File Path" Binding="{Binding FilePath}" Width="*" MinWidth="200" IsReadOnly="True" CanUserResize="True"/>
                    </DataGrid.Columns>
                    <DataGrid.ContextMenu>
                        <ContextMenu>
                            <MenuItem Header="Kill Process" Command="{Binding KillProcessCommand}" />
                            <MenuItem Header="Load Details" Command="{Binding LoadDetailsCommand}" CommandParameter="{Binding PlacementTarget.SelectedItem, RelativeSource={RelativeSource AncestorType=ContextMenu}}" />
                            <Separator />
                            <MenuItem Header="Open File Location" />
                        </ContextMenu>
                    </DataGrid.ContextMenu>
                </DataGrid>
        </GroupBox>

        <!-- Status Bar -->
        <StatusBar Grid.Row="3">
            <StatusBarItem>
                <TextBlock Text="{Binding DataContext.StatusMessage, RelativeSource={RelativeSource AncestorType=UserControl}}" />
            </StatusBarItem>
            <StatusBarItem HorizontalAlignment="Right">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Refreshing..." Margin="0,0,8,0"
                               Visibility="{Binding DataContext.IsRefreshing, RelativeSource={RelativeSource AncestorType=UserControl}, Converter={StaticResource BooleanToVisibilityConverter}, FallbackValue=Collapsed, TargetNullValue=Collapsed}" />
                    <ProgressBar Width="100" Height="16" IsIndeterminate="True"
                                 Visibility="{Binding DataContext.IsRefreshing, RelativeSource={RelativeSource AncestorType=UserControl}, Converter={StaticResource BooleanToVisibilityConverter}, FallbackValue=Collapsed, TargetNullValue=Collapsed}" />
                </StackPanel>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</UserControl>
