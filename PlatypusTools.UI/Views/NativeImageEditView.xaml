<UserControl x:Class="PlatypusTools.UI.Views.NativeImageEditView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="700" d:DesignWidth="900">
    <Grid Margin="5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- File Selection -->
        <GroupBox Header="Image File" Grid.Row="0" Margin="0,0,0,5">
            <Grid Margin="5">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="File:" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <TextBox x:Name="ImageFilePathBox" Grid.Column="1" IsReadOnly="True" Margin="0,0,10,0"/>
                <Button Grid.Column="2" Content="Browse..." Click="BrowseImage_Click" Width="80" Padding="5"/>
            </Grid>
        </GroupBox>

        <!-- Quick Actions -->
        <GroupBox Header="Quick Image Operations" Grid.Row="1" Margin="0,0,0,5">
            <WrapPanel Margin="10">
                <Button Content="↶ Rotate Left" Click="RotateLeft_Click" Width="120" Height="35" Margin="0,0,5,5"/>
                <Button Content="↷ Rotate Right" Click="RotateRight_Click" Width="120" Height="35" Margin="0,0,5,5"/>
                <Button Content="↔ Flip Horizontal" Click="FlipH_Click" Width="130" Height="35" Margin="0,0,5,5"/>
                <Button Content="↕ Flip Vertical" Click="FlipV_Click" Width="120" Height="35" Margin="0,0,5,5"/>
                <Button Content="🎨 Grayscale" Click="Grayscale_Click" Width="120" Height="35" Margin="0,0,5,5"/>
                <Button Content="🔄 Invert Colors" Click="Invert_Click" Width="120" Height="35" Margin="0,0,5,5"/>
            </WrapPanel>
        </GroupBox>
        
        <!-- Image Preview -->
        <GroupBox Header="Image Preview" Grid.Row="2">
            <Grid>
                <Image x:Name="ImagePreview" Stretch="Uniform" Margin="5"/>
                <TextBlock x:Name="PlaceholderText" Text="Select an image file to edit" 
                           HorizontalAlignment="Center" VerticalAlignment="Center"
                           Foreground="#999" FontSize="14" FontStyle="Italic"/>
            </Grid>
        </GroupBox>
        
        <!-- Save Options -->
        <GroupBox Header="Save Options" Grid.Row="3" Margin="0,5,0,0">
            <Grid Margin="10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="Output Format:" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <ComboBox x:Name="OutputFormat" Grid.Column="1" SelectedIndex="0" Width="100">
                    <ComboBoxItem Content="PNG"/>
                    <ComboBoxItem Content="JPEG"/>
                    <ComboBoxItem Content="BMP"/>
                    <ComboBoxItem Content="GIF"/>
                    <ComboBoxItem Content="WEBP"/>
                </ComboBox>
                <TextBlock x:Name="StatusText" Grid.Column="2" Text="" Foreground="Blue" VerticalAlignment="Center" Margin="20,0,0,0"/>
                <Button Grid.Column="3" Content="💾 Save As..." Click="Save_Click" Width="120" Height="35" Margin="10,0,5,0" FontWeight="Bold" Background="LightGreen"/>
                <Button Grid.Column="4" Content="↩ Reset" Click="Reset_Click" Width="80" Height="35"/>
            </Grid>
        </GroupBox>
    </Grid>
</UserControl>
