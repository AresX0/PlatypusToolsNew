<Window x:Class="PlatypusTools.UI.Views.ActivityLogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Activity Log" Height="560" Width="900"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
            <TextBlock Text="Filter:" VerticalAlignment="Center" Margin="0,0,6,0"/>
            <TextBox x:Name="FilterBox" Width="220" TextChanged="Filter_Changed"/>
            <TextBlock Text="Level:" VerticalAlignment="Center" Margin="12,0,6,0"/>
            <ComboBox x:Name="LevelBox" Width="120" SelectionChanged="Filter_Changed">
                <ComboBoxItem Content="All" IsSelected="True"/>
                <ComboBoxItem Content="Debug"/>
                <ComboBoxItem Content="Info"/>
                <ComboBoxItem Content="Warning"/>
                <ComboBoxItem Content="Error"/>
            </ComboBox>
            <CheckBox x:Name="AutoScrollBox" Content="Auto-scroll" IsChecked="True" Margin="12,0,0,0" VerticalAlignment="Center"/>
            <Button Content="Refresh from disk" Margin="12,0,0,0" Padding="8,2" Click="Refresh_Click"/>
            <Button Content="Open log folder" Margin="6,0,0,0" Padding="8,2" Click="OpenFolder_Click"/>
            <Button Content="Clear" Margin="6,0,0,0" Padding="8,2" Click="Clear_Click"/>
        </StackPanel>

        <DataGrid x:Name="LogGrid" Grid.Row="1" AutoGenerateColumns="False"
                  IsReadOnly="True" CanUserAddRows="False"
                  SelectionMode="Extended" SelectionUnit="FullRow">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Time" Binding="{Binding UtcStamp, StringFormat=HH:mm:ss}" Width="80"/>
                <DataGridTextColumn Header="Level" Binding="{Binding Level}" Width="80"/>
                <DataGridTextColumn Header="Category" Binding="{Binding Category}" Width="140"/>
                <DataGridTextColumn Header="Message" Binding="{Binding Message}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <StatusBar Grid.Row="2">
            <TextBlock x:Name="StatusText" Text=""/>
        </StatusBar>
    </Grid>
</Window>
