<Window x:Class="PlatypusTools.UI.Views.UpdateWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:PlatypusTools.UI.ViewModels"
        Title="Update Available — What's New" Height="500" Width="600"
        WindowStartupLocation="CenterOwner"
        MinHeight="350" MinWidth="450"
        ResizeMode="CanResize">
    
    <Window.DataContext>
        <vm:UpdateViewModel/>
    </Window.DataContext>
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,16">
            <TextBlock Text="🔄" FontSize="32" VerticalAlignment="Center"/>
            <StackPanel Margin="12,0,0,0">
                <TextBlock Text="Update Available!" FontSize="20" FontWeight="Bold"/>
                <TextBlock Text="{Binding UpdateMessage}" Foreground="Gray"/>
            </StackPanel>
        </StackPanel>
        
        <!-- Version Info -->
        <Grid Grid.Row="1" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <TextBlock Grid.Row="0" Grid.Column="0" Text="Current Version:" FontWeight="SemiBold" Margin="0,0,12,4"/>
            <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding CurrentVersion, FallbackValue='2.0.0.17'}"/>
            
            <TextBlock Grid.Row="1" Grid.Column="0" Text="New Version:" FontWeight="SemiBold" Margin="0,0,12,4"/>
            <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding UpdateAvailable.Version}"/>
            
            <TextBlock Grid.Row="2" Grid.Column="0" Text="Download Size:" FontWeight="SemiBold" Margin="0,0,12,4"/>
            <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding UpdateAvailable.FileSizeDisplay}"/>
        </Grid>
        
        <!-- Release Notes -->
        <GroupBox Grid.Row="2" Header="Release Notes" Margin="0,0,0,16">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <TextBlock Text="{Binding UpdateAvailable.Body}" TextWrapping="Wrap" Margin="8"/>
            </ScrollViewer>
        </GroupBox>
        
        <!-- Progress -->
        <StackPanel Grid.Row="3" Margin="0,0,0,16" 
                    Visibility="{Binding IsDownloading, Converter={StaticResource BoolToVisibilityConverter}}">
            <TextBlock Text="Downloading update..." Margin="0,0,0,4"/>
            <ProgressBar Value="{Binding DownloadProgress}" Height="20"/>
            <TextBlock Text="{Binding DownloadProgress, StringFormat={}{0:F1}%}" 
                       HorizontalAlignment="Center" Margin="0,4,0,0"/>
        </StackPanel>
        
        <!-- Buttons -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="View Release Page" 
                    Command="{Binding ViewReleaseNotesCommand}"
                    Margin="0,0,8,0" Padding="16,8"/>
            
            <Button Content="Download &amp; Install" 
                    Command="{Binding DownloadUpdateCommand}"
                    Margin="0,0,8,0" Padding="16,8"
                    Visibility="{Binding IsDownloading, Converter={StaticResource InverseBoolToVisibilityConverter}}"/>
            
            <Button Content="Install Now" 
                    Command="{Binding InstallUpdateCommand}"
                    Margin="0,0,8,0" Padding="16,8"
                    Visibility="{Binding DownloadedFilePath, Converter={StaticResource StringToVisibilityConverter}}"/>
            
            <Button Content="Later" 
                    Command="{Binding DismissCommand}"
                    IsCancel="True" Padding="16,8"/>
        </StackPanel>
    </Grid>
</Window>
