<UserControl x:Class="PlatypusTools.UI.Views.CloudSyncView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="900">
    
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="5"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <TextBlock Grid.Row="0" Text="☁ Cloud Sync" FontSize="18" FontWeight="Bold" Margin="0,0,0,8"/>
        
        <!-- Cloud Providers -->
        <GroupBox Grid.Row="1" Header="Cloud Providers" Margin="0,0,0,5">
            <Grid Margin="5">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                
                <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                    <Button Content="🔍 Detect Providers" Click="DetectProviders_Click" Padding="8,4" Margin="0,0,5,0"/>
                    <Button Content="📁 Add Local Folder" Click="AddLocalProvider_Click" Padding="8,4" Margin="0,0,5,0"/>
                    <Button Content="🔄 Sync Now" Click="SyncNow_Click" Padding="8,4" Margin="0,0,5,0"
                            Background="#FF4CAF50" Foreground="White"/>
                    <Button Content="⏹ Stop" Click="StopSync_Click" Padding="8,4"
                            Background="#FFF44336" Foreground="White"/>
                </StackPanel>
                
                <DataGrid x:Name="ProvidersGrid" Grid.Row="1" 
                          AutoGenerateColumns="False" IsReadOnly="True"
                          MaxHeight="120" CanUserSortColumns="True"
                          GridLinesVisibility="Horizontal">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="150"/>
                        <DataGridTextColumn Header="Type" Binding="{Binding Type}" Width="100"/>
                        <DataGridTextColumn Header="Path" Binding="{Binding RootPath}" Width="*"/>
                        <DataGridTextColumn Header="Status" Binding="{Binding IsConnected}" Width="80"/>
                        <DataGridTextColumn Header="Last Sync" Binding="{Binding LastSync, StringFormat='{}{0:g}'}" Width="140"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </GroupBox>
        
        <GridSplitter Grid.Row="2" Height="5" HorizontalAlignment="Stretch" Background="#CCCCCC" Cursor="SizeNS"/>
        
        <!-- Sync Rules -->
        <GroupBox Grid.Row="3" Header="Sync Rules">
            <Grid Margin="5">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                
                <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                    <Button Content="➕ Add Rule" Click="AddSyncRule_Click" Padding="8,4" Margin="0,0,5,0"/>
                    <Button Content="✕ Remove Rule" Click="RemoveSyncRule_Click" Padding="8,4" Margin="0,0,5,0"/>
                    <Button Content="✏️ Edit Rule" Click="EditSyncRule_Click" Padding="8,4"/>
                </StackPanel>
                
                <DataGrid x:Name="SyncRulesGrid" Grid.Row="1" 
                          AutoGenerateColumns="False" IsReadOnly="True"
                          CanUserSortColumns="True" GridLinesVisibility="Horizontal">
                    <DataGrid.Columns>
                        <DataGridCheckBoxColumn Header="" Binding="{Binding IsEnabled}" Width="30"/>
                        <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="150"/>
                        <DataGridTextColumn Header="Local Path" Binding="{Binding LocalPath}" Width="*"/>
                        <DataGridTextColumn Header="Remote Path" Binding="{Binding RemotePath}" Width="*"/>
                        <DataGridTextColumn Header="Direction" Binding="{Binding Direction}" Width="100"/>
                        <DataGridTextColumn Header="Interval" Binding="{Binding SyncIntervalMinutes, StringFormat='{}{0} min'}" Width="80"/>
                        <DataGridTextColumn Header="Last Sync" Binding="{Binding LastSync, StringFormat='{}{0:g}'}" Width="140"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </GroupBox>
        
        <!-- Status bar -->
        <Border Grid.Row="4" Background="#F0F0F0" Padding="5,3" Margin="0,5,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock x:Name="StatusText" Text="Ready" FontSize="11"/>
                <ProgressBar x:Name="SyncProgress" Grid.Column="1" Width="200" Height="14" 
                             Minimum="0" Maximum="100" Value="0"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>
