<UserControl x:Class="PlatypusTools.UI.Views.ImageResizerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:PlatypusTools.UI.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:ImageResizerViewModel}">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- File Management -->
        <GroupBox Header="Image Files" Grid.Row="0" Margin="0,0,0,10">
            <StackPanel>
                <StackPanel Orientation="Horizontal" Margin="5" HorizontalAlignment="Left">
                    <Button Content="Add Files" Command="{Binding AddFilesCommand}" Width="100" Margin="0,0,10,0" Padding="5"/>
                    <Button Content="Clear List" Command="{Binding ClearListCommand}" Width="100" Padding="5"/>
                    <Separator Width="20" Background="Transparent"/>
                    <Button Content="Select All" Command="{Binding SelectAllCommand}" Width="80" Margin="0,0,5,0" Padding="3"/>
                    <Button Content="Select None" Command="{Binding SelectNoneCommand}" Width="80" Padding="3"/>
                </StackPanel>
            </StackPanel>
        </GroupBox>

        <!-- File List -->
        <GroupBox Header="Images to Resize" Grid.Row="1" Margin="0,0,0,10">
            <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
                <DataGrid ItemsSource="{Binding FileList}" AutoGenerateColumns="False" 
                          CanUserAddRows="False" CanUserDeleteRows="False" Margin="5"
                          CanUserResizeColumns="True" CanUserSortColumns="True"
                          ColumnHeaderHeight="32"
                          HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Disabled">
                    <DataGrid.Columns>
                        <DataGridCheckBoxColumn Header="Process" Binding="{Binding Process, UpdateSourceTrigger=PropertyChanged}" Width="70" CanUserResize="True" MinWidth="60"/>
                        <DataGridTextColumn Header="Name" Binding="{Binding Name, Mode=OneWay}" Width="250" IsReadOnly="True" CanUserResize="True" MinWidth="100"/>
                        <DataGridTextColumn Header="Dimensions" Binding="{Binding Dimensions, Mode=OneWay}" Width="100" IsReadOnly="True" CanUserResize="True" MinWidth="80"/>
                        <DataGridTextColumn Header="Size" Binding="{Binding Size, Mode=OneWay}" Width="80" IsReadOnly="True" CanUserResize="True" MinWidth="60"/>
                        <DataGridTextColumn Header="Path" Binding="{Binding Path, Mode=OneWay}" Width="400" IsReadOnly="True" CanUserResize="True" MinWidth="150"/>
                        <DataGridTextColumn Header="Status" Binding="{Binding Status, Mode=OneWay}" Width="200" IsReadOnly="True" CanUserResize="True" MinWidth="100"/>
                    </DataGrid.Columns>
                </DataGrid>
            </ScrollViewer>
        </GroupBox>

        <!-- Resize Options -->
        <GroupBox Header="Resize Options" Grid.Row="2" Margin="0,0,0,10">
            <StackPanel Margin="5">
                <!-- Output Folder -->
                <Grid Margin="0,0,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0" Text="Output Folder:" VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <TextBox Grid.Column="1" Text="{Binding OutputFolder, UpdateSourceTrigger=PropertyChanged}" 
                             Margin="0,0,10,0"/>
                    <Button Grid.Column="2" Content="Browse" Command="{Binding BrowseOutputFolderCommand}" 
                            Width="80" Padding="5"/>
                </Grid>

                <!-- Dimensions and Quality -->
                <Grid Margin="0,0,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="100"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="100"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="100"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0" Text="Max Width:" VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <TextBox Grid.Column="1" Text="{Binding MaxWidth, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,20,0"/>

                    <TextBlock Grid.Column="2" Text="Max Height:" VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <TextBox Grid.Column="3" Text="{Binding MaxHeight, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,20,0"/>

                    <TextBlock Grid.Column="4" Text="Quality:" VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <TextBox Grid.Column="5" Text="{Binding Quality, UpdateSourceTrigger=PropertyChanged}"/>
                </Grid>

                <!-- Format and Checkboxes -->
                <Grid Margin="0,0,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="150"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0" Text="Format:" VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <ComboBox Grid.Column="1" SelectedItem="{Binding SelectedFormat, UpdateSourceTrigger=PropertyChanged}">
                        <ComboBoxItem Content="Same" IsSelected="True"/>
                        <ComboBoxItem Content="JPG"/>
                        <ComboBoxItem Content="PNG"/>
                        <ComboBoxItem Content="BMP"/>
                        <ComboBoxItem Content="GIF"/>
                        <ComboBoxItem Content="TIFF"/>
                    </ComboBox>

                    <StackPanel Grid.Column="2" Orientation="Horizontal" Margin="20,0,0,0">
                        <CheckBox Content="Overwrite Existing" IsChecked="{Binding OverwriteExisting}" Margin="0,0,20,0" VerticalAlignment="Center"/>
                        <CheckBox Content="Maintain Aspect Ratio" IsChecked="{Binding MaintainAspectRatio}" VerticalAlignment="Center"/>
                    </StackPanel>
                </Grid>

                <!-- Resize Button -->
                <Button Content="Resize Images" Command="{Binding ResizeImagesCommand}" 
                        Width="150" HorizontalAlignment="Left" Padding="8" FontWeight="Bold"/>
            </StackPanel>
        </GroupBox>

        <!-- Status Bar -->
        <Border Grid.Row="3" Background="#F0F0F0" BorderBrush="#CCCCCC" BorderThickness="1" Padding="10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="{Binding StatusMessage}" VerticalAlignment="Center" FontWeight="SemiBold"/>
                <Button Grid.Column="1" Content="Cancel" Command="{Binding CancelCommand}" 
                        Width="80" Padding="5" Visibility="{Binding IsResizing, Converter={StaticResource BooleanToVisibilityConverter}}"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>
