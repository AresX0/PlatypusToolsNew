<Window x:Class="PlatypusTools.UI.Views.DependencySetupWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="PlatypusTools - Dependency Setup" Height="550" Width="650"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource WindowBackgroundBrush}"
        Foreground="{DynamicResource WindowForegroundBrush}">
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="🔧 Welcome to PlatypusTools!" FontSize="24" FontWeight="Bold" Margin="0,0,0,8"/>
            <TextBlock Text="Some features require external tools to work properly. You can install them now or later from Settings." 
                       TextWrapping="Wrap" Foreground="Gray"/>
        </StackPanel>
        
        <!-- Status Message -->
        <Border Grid.Row="1" Background="{DynamicResource SurfaceBrush}" CornerRadius="8" Padding="16" Margin="0,0,0,16">
            <StackPanel>
                <TextBlock x:Name="StatusMessage" Text="Checking dependencies..." FontSize="14"/>
            </StackPanel>
        </Border>
        
        <!-- Dependencies List -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <StackPanel x:Name="DependenciesPanel">
                <!-- FFmpeg -->
                <Border x:Name="FFmpegCard" Background="{DynamicResource SurfaceBrush}" CornerRadius="8" Padding="16" Margin="0,0,0,12">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock x:Name="FFmpegStatus" Text="⏳" FontSize="16" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                <TextBlock Text="FFmpeg" FontSize="16" FontWeight="SemiBold"/>
                            </StackPanel>
                            <TextBlock Text="Required for: Video editing, conversion, screen recording, audio processing" 
                                       Foreground="Gray" TextWrapping="Wrap" Margin="24,4,0,0"/>
                            <TextBlock x:Name="FFmpegPath" Text="" Foreground="Green" FontSize="11" Margin="24,4,0,0"/>
                        </StackPanel>
                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                            <Button x:Name="FFmpegInstallBtn" Content="Install" MinWidth="180" Click="InstallFFmpeg_Click"/>
                            <Button x:Name="FFmpegManualBtn" Content="Manual ↗" MinWidth="180" Margin="0,4,0,0" Click="OpenFFmpegSite_Click"/>
                        </StackPanel>
                    </Grid>
                </Border>
                
                <!-- ExifTool -->
                <Border x:Name="ExifToolCard" Background="{DynamicResource SurfaceBrush}" CornerRadius="8" Padding="16" Margin="0,0,0,12">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock x:Name="ExifToolStatus" Text="⏳" FontSize="16" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                <TextBlock Text="ExifTool" FontSize="16" FontWeight="SemiBold"/>
                            </StackPanel>
                            <TextBlock Text="Required for: Metadata editing, EXIF data reading/writing" 
                                       Foreground="Gray" TextWrapping="Wrap" Margin="24,4,0,0"/>
                            <TextBlock x:Name="ExifToolPath" Text="" Foreground="Green" FontSize="11" Margin="24,4,0,0"/>
                        </StackPanel>
                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                            <Button x:Name="ExifToolInstallBtn" Content="Install" MinWidth="180" Click="InstallExifTool_Click"/>
                            <Button x:Name="ExifToolManualBtn" Content="Manual ↗" MinWidth="180" Margin="0,4,0,0" Click="OpenExifToolSite_Click"/>
                        </StackPanel>
                    </Grid>
                </Border>
                
                <!-- WebView2 -->
                <Border x:Name="WebView2Card" Background="{DynamicResource SurfaceBrush}" CornerRadius="8" Padding="16" Margin="0,0,0,12">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock x:Name="WebView2Status" Text="⏳" FontSize="16" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                <TextBlock Text="WebView2 Runtime" FontSize="16" FontWeight="SemiBold"/>
                            </StackPanel>
                            <TextBlock Text="Required for: Help system, simple browser, PDF viewing" 
                                       Foreground="Gray" TextWrapping="Wrap" Margin="24,4,0,0"/>
                            <TextBlock x:Name="WebView2Path" Text="" Foreground="Green" FontSize="11" Margin="24,4,0,0"/>
                        </StackPanel>
                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                            <Button x:Name="WebView2InstallBtn" Content="Install" MinWidth="180" Click="InstallWebView2_Click"/>
                            <Button x:Name="WebView2ManualBtn" Content="Manual ↗" MinWidth="180" Margin="0,4,0,0" Click="OpenWebView2Site_Click"/>
                        </StackPanel>
                    </Grid>
                </Border>
                
                <!-- Manual Install Info -->
                <Border Background="#2D2D30" CornerRadius="8" Padding="16" Margin="0,8,0,0">
                    <StackPanel>
                        <TextBlock Text="📁 Manual Installation Locations" FontWeight="SemiBold" Margin="0,0,0,8"/>
                        <TextBlock TextWrapping="Wrap" Foreground="Gray">
                            <Run Text="If automatic installation fails, download the tools manually and place them in:"/>
                        </TextBlock>
                        <TextBlock x:Name="ToolsPathText" Text="" FontFamily="Consolas" Foreground="#569CD6" Margin="0,8,0,0"/>
                        <TextBlock TextWrapping="Wrap" Foreground="Gray" Margin="0,8,0,0">
                            <Run Text="• FFmpeg: Place ffmpeg.exe in the Tools folder or add to system PATH"/>
                            <LineBreak/>
                            <Run Text="• ExifTool: Place exiftool.exe in Tools\exiftool_files\ folder"/>
                            <LineBreak/>
                            <Run Text="• WebView2: Run the installer from Microsoft"/>
                        </TextBlock>
                        <Button Content="Open Tools Folder" MinWidth="180" HorizontalAlignment="Left" Margin="0,12,0,0" Click="OpenToolsFolder_Click"/>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>
        
        <!-- Options -->
        <StackPanel Grid.Row="3" Margin="0,16,0,0">
            <CheckBox x:Name="DontShowAgainCheck" Content="Don't show this again on startup" Margin="0,0,0,8"/>
        </StackPanel>
        
        <!-- Buttons -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="Refresh Status" MinWidth="180" Margin="0,0,8,0" Click="RefreshStatus_Click"/>
            <Button Content="Continue" MinWidth="180" Click="Continue_Click" IsDefault="True" 
                    Background="#FF4CAF50" Foreground="White"/>
        </StackPanel>
    </Grid>
</Window>
