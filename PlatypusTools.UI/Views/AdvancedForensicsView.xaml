<UserControl x:Class="PlatypusTools.UI.Views.AdvancedForensicsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:PlatypusTools.UI.ViewModels">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,10">
            <TextBlock Text="🔬 Advanced Forensics (DFIR)" FontSize="24" FontWeight="Bold"/>
            <TextBlock Text="Digital Forensics &amp; Incident Response - Memory acquisition &amp; analysis, artifact collection, timeline creation, malware analysis, and OSINT" 
                       Foreground="Gray" TextWrapping="Wrap"/>
        </StackPanel>

        <!-- Main Content -->
        <TabControl Grid.Row="1">
            
            <!-- Memory Acquisition &amp; Analysis Tab -->
            <TabItem Header="🧠 Memory">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="10">
                        
                        <!-- Memory Acquisition Section -->
                        <GroupBox Header="💾 Memory Acquisition (WinPmem/DumpIt)" Margin="0,0,0,10">
                            <StackPanel Margin="5">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="120"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>

                                    <TextBlock Text="WinPmem Path:" VerticalAlignment="Center" Margin="5"/>
                                    <TextBox Grid.Column="1" Text="{Binding WinPmemPath, UpdateSourceTrigger=PropertyChanged}" Margin="5"/>
                                    <Button Grid.Column="2" Content="Browse" Command="{Binding BrowseWinPmemCommand}" Width="80" Margin="5"/>

                                    <TextBlock Grid.Row="1" Text="Output Folder:" VerticalAlignment="Center" Margin="5"/>
                                    <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding DumpOutputPath, UpdateSourceTrigger=PropertyChanged}" Margin="5"/>
                                    <Button Grid.Row="1" Grid.Column="2" Content="Browse" Command="{Binding BrowseDumpOutputCommand}" Width="80" Margin="5"/>

                                    <TextBlock Grid.Row="2" Text="Format:" VerticalAlignment="Center" Margin="5"/>
                                    <ComboBox Grid.Row="2" Grid.Column="1" ItemsSource="{Binding DumpFormats}" 
                                              SelectedItem="{Binding DumpFormat}" Margin="5" Width="150" HorizontalAlignment="Left"/>
                                </Grid>
                                
                                <TextBlock Text="⚠️ Memory acquisition requires Administrator privileges" Foreground="Orange" Margin="5"/>
                                
                                <Button Content="📥 Acquire Live Memory Dump" Command="{Binding AcquireMemoryDumpCommand}"
                                        Height="35" FontSize="12" FontWeight="Bold" 
                                        Background="#E91E63" Foreground="White" Margin="0,5,0,0"
                                        IsEnabled="{Binding IsRunning, Converter={StaticResource InverseBoolConverter}}"/>
                            </StackPanel>
                        </GroupBox>

                        <!-- Memory Analysis Section -->
                        <GroupBox Header="🔍 Memory Analysis (Volatility 3)" Margin="0,0,0,10">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="120"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <TextBlock Text="Memory Dump:" VerticalAlignment="Center" Margin="5"/>
                                <TextBox Grid.Column="1" Text="{Binding MemoryDumpPath, UpdateSourceTrigger=PropertyChanged}" Margin="5"/>
                                <Button Grid.Column="2" Content="Browse" Command="{Binding BrowseMemoryDumpCommand}" Width="80" Margin="5"/>

                                <TextBlock Grid.Row="1" Text="Volatility Path:" VerticalAlignment="Center" Margin="5"/>
                                <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding VolatilityPath, UpdateSourceTrigger=PropertyChanged}" Margin="5"/>
                                <Button Grid.Row="1" Grid.Column="2" Content="Browse" Command="{Binding BrowseVolatilityCommand}" Width="80" Margin="5"/>

                                <TextBlock Grid.Row="2" Text="Output Path:" VerticalAlignment="Center" Margin="5"/>
                                <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding MemoryOutputPath, UpdateSourceTrigger=PropertyChanged}" Margin="5"/>
                            </Grid>
                        </GroupBox>

                        <GroupBox Header="Volatility 3 Plugins" Margin="0,0,0,10">
                            <StackPanel>
                                <TextBlock Text="Core Analysis" FontWeight="SemiBold" Margin="5,5,5,2"/>
                                <WrapPanel Margin="5,0,5,5">
                                    <CheckBox Content="windows.pslist (Processes)" IsChecked="{Binding RunPsList}" Margin="10,5"/>
                                    <CheckBox Content="windows.netscan (Network)" IsChecked="{Binding RunNetScan}" Margin="10,5"/>
                                    <CheckBox Content="windows.malfind (Malware)" IsChecked="{Binding RunMalfind}" Margin="10,5"/>
                                    <CheckBox Content="windows.dlllist (DLLs)" IsChecked="{Binding RunDllList}" Margin="10,5"/>
                                    <CheckBox Content="windows.handles" IsChecked="{Binding RunHandles}" Margin="10,5"/>
                                    <CheckBox Content="windows.cmdline" IsChecked="{Binding RunCmdline}" Margin="10,5"/>
                                    <CheckBox Content="windows.filescan" IsChecked="{Binding RunFileScan}" Margin="10,5"/>
                                    <CheckBox Content="windows.registry.hivelist" IsChecked="{Binding RunRegHive}" Margin="10,5"/>
                                </WrapPanel>
                                <TextBlock Text="Advanced Analysis" FontWeight="SemiBold" Margin="5,5,5,2"/>
                                <WrapPanel Margin="5,0,5,5">
                                    <CheckBox Content="windows.pstree (Process Tree)" IsChecked="{Binding RunPsTree}" Margin="10,5"/>
                                    <CheckBox Content="windows.envars (Environment)" IsChecked="{Binding RunEnvars}" Margin="10,5"/>
                                    <CheckBox Content="windows.svcscan (Services)" IsChecked="{Binding RunSvcScan}" Margin="10,5"/>
                                    <CheckBox Content="windows.callbacks" IsChecked="{Binding RunCallbacks}" Margin="10,5"/>
                                    <CheckBox Content="windows.driverscan" IsChecked="{Binding RunDriverScan}" Margin="10,5"/>
                                    <CheckBox Content="windows.ssdt (SSDT Hooks)" IsChecked="{Binding RunSsdt}" Margin="10,5"/>
                                    <CheckBox Content="windows.mutantscan (Mutexes)" IsChecked="{Binding RunMutantScan}" Margin="10,5"/>
                                </WrapPanel>
                            </StackPanel>
                        </GroupBox>

                        <Button Content="🔍 Run Volatility Analysis" Command="{Binding RunVolatilityAnalysisCommand}"
                                Height="40" FontSize="14" FontWeight="Bold" 
                                Background="#2196F3" Foreground="White"
                                IsEnabled="{Binding IsRunning, Converter={StaticResource InverseBoolConverter}}"/>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>

            <!-- Artifact Collection Tab -->
            <TabItem Header="📦 Artifacts">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="10">
                        <GroupBox Header="KAPE / EZ Tools Configuration" Margin="0,0,0,10">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="120"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <TextBlock Text="KAPE Path:" VerticalAlignment="Center" Margin="5"/>
                                <TextBox Grid.Column="1" Text="{Binding KapePath, UpdateSourceTrigger=PropertyChanged}" Margin="5"/>
                                <Button Grid.Column="2" Content="Browse" Command="{Binding BrowseKapeCommand}" Width="80" Margin="5"/>

                                <TextBlock Grid.Row="1" Text="Target Path:" VerticalAlignment="Center" Margin="5"/>
                                <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding KapeTargetPath, UpdateSourceTrigger=PropertyChanged}" Margin="5"/>
                                <Button Grid.Row="1" Grid.Column="2" Content="Browse" Command="{Binding BrowseKapeTargetCommand}" Width="80" Margin="5"/>

                                <TextBlock Grid.Row="2" Text="Output Path:" VerticalAlignment="Center" Margin="5"/>
                                <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding KapeOutputPath, UpdateSourceTrigger=PropertyChanged}" Margin="5"/>
                            </Grid>
                        </GroupBox>

                        <GroupBox Header="Core Artifacts" Margin="0,0,0,10">
                            <WrapPanel Margin="5">
                                <CheckBox Content="Prefetch" IsChecked="{Binding CollectPrefetch}" Margin="10,5" ToolTip="Execution evidence"/>
                                <CheckBox Content="Amcache" IsChecked="{Binding CollectAmcache}" Margin="10,5" ToolTip="Program presence"/>
                                <CheckBox Content="Event Logs" IsChecked="{Binding CollectEventLogs}" Margin="10,5" ToolTip="Security, Sysmon, PowerShell"/>
                                <CheckBox Content="$MFT" IsChecked="{Binding CollectMft}" Margin="10,5" ToolTip="Master File Table"/>
                                <CheckBox Content="Registry" IsChecked="{Binding CollectRegistry}" Margin="10,5" ToolTip="SAM, SYSTEM, SOFTWARE"/>
                                <CheckBox Content="SRUM" IsChecked="{Binding CollectSrum}" Margin="10,5" ToolTip="System Resource Usage"/>
                            </WrapPanel>
                        </GroupBox>

                        <GroupBox Header="Additional Artifacts" Margin="0,0,0,10">
                            <WrapPanel Margin="5">
                                <CheckBox Content="ShellBags" IsChecked="{Binding CollectShellBags}" Margin="10,5" ToolTip="Folder access history"/>
                                <CheckBox Content="Jump Lists" IsChecked="{Binding CollectJumpLists}" Margin="10,5" ToolTip="Recent files per app"/>
                                <CheckBox Content="LNK Files" IsChecked="{Binding CollectLnkFiles}" Margin="10,5" ToolTip="Shortcut files"/>
                                <CheckBox Content="Shimcache" IsChecked="{Binding CollectShimcache}" Margin="10,5" ToolTip="App compatibility cache"/>
                                <CheckBox Content="Recycle Bin" IsChecked="{Binding CollectRecycleBin}" Margin="10,5" ToolTip="Deleted files"/>
                                <CheckBox Content="$UsnJrnl" IsChecked="{Binding CollectUsnJrnl}" Margin="10,5" ToolTip="File change journal"/>
                            </WrapPanel>
                        </GroupBox>

                        <TextBlock Text="⚠️ Note: Some artifacts require Administrator privileges" Foreground="Orange" Margin="5"/>

                        <Button Content="📦 Collect Artifacts" Command="{Binding RunKapeCollectionCommand}"
                                Height="40" FontSize="14" FontWeight="Bold" 
                                Background="#4CAF50" Foreground="White" Margin="0,10,0,0"
                                IsEnabled="{Binding IsRunning, Converter={StaticResource InverseBoolConverter}}"/>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>

            <!-- Timeline Tab (Plaso/Velociraptor) -->
            <TabItem Header="📅 Timeline">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="10">
                        
                        <!-- Plaso Section -->
                        <GroupBox Header="🕐 Plaso (log2timeline) - Super-Timeline" Margin="0,0,0,10">
                            <StackPanel Margin="5">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="120"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>

                                    <TextBlock Text="Plaso Path:" VerticalAlignment="Center" Margin="5"/>
                                    <TextBox Grid.Column="1" Text="{Binding PlasoPath, UpdateSourceTrigger=PropertyChanged}" Margin="5"/>
                                    <Button Grid.Column="2" Content="Browse" Command="{Binding BrowsePlasoCommand}" Width="80" Margin="5"/>

                                    <TextBlock Grid.Row="1" Text="Evidence Path:" VerticalAlignment="Center" Margin="5"/>
                                    <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding PlasoEvidencePath, UpdateSourceTrigger=PropertyChanged}" Margin="5"/>
                                    <Button Grid.Row="1" Grid.Column="2" Content="Browse" Command="{Binding BrowseEvidenceCommand}" Width="80" Margin="5"/>

                                    <TextBlock Grid.Row="2" Text="Storage File:" VerticalAlignment="Center" Margin="5"/>
                                    <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding PlasoStorageFile, UpdateSourceTrigger=PropertyChanged}" Margin="5"/>
                                </Grid>

                                <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                                    <Button Content="🕐 Create Timeline" Command="{Binding RunPlasoTimelineCommand}"
                                            Height="35" Width="150" Margin="0,0,10,0"
                                            Background="#673AB7" Foreground="White"
                                            IsEnabled="{Binding IsRunning, Converter={StaticResource InverseBoolConverter}}"/>
                                    <Button Content="📤 Export to OpenSearch" Command="{Binding ExportPlasoToOpenSearchCommand}"
                                            Height="35" Width="180"
                                            IsEnabled="{Binding IsRunning, Converter={StaticResource InverseBoolConverter}}"/>
                                </StackPanel>
                            </StackPanel>
                        </GroupBox>

                        <!-- Velociraptor Section -->
                        <GroupBox Header="🦖 Velociraptor - Fleet Triage" Margin="0,0,0,10">
                            <StackPanel Margin="5">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="120"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>

                                    <TextBlock Text="Velociraptor:" VerticalAlignment="Center" Margin="5"/>
                                    <TextBox Grid.Column="1" Text="{Binding VelociraptorPath, UpdateSourceTrigger=PropertyChanged}" Margin="5"/>
                                    <Button Grid.Column="2" Content="Browse" Command="{Binding BrowseVelociraptorCommand}" Width="80" Margin="5"/>

                                    <TextBlock Grid.Row="1" Text="Artifact:" VerticalAlignment="Center" Margin="5"/>
                                    <ComboBox Grid.Row="1" Grid.Column="1" ItemsSource="{Binding VelociraptorArtifacts}" 
                                              SelectedItem="{Binding VelociraptorArtifact}" Margin="5"/>
                                </Grid>

                                <Button Content="🦖 Collect Artifact" Command="{Binding RunVelociraptorCollectCommand}"
                                        Height="35" Width="150" HorizontalAlignment="Left" Margin="0,10,0,0"
                                        Background="#795548" Foreground="White"
                                        IsEnabled="{Binding IsRunning, Converter={StaticResource InverseBoolConverter}}"/>
                            </StackPanel>
                        </GroupBox>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>

            <!-- Kusto Queries Tab -->
            <TabItem Header="📊 Kusto">
                <Grid Margin="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <GroupBox Header="Azure Data Explorer / Log Analytics Connection">
                        <Grid Margin="5">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <TextBlock Text="Endpoint:" VerticalAlignment="Center" Margin="5"/>
                            <TextBox Grid.Column="1" Text="{Binding KustoEndpoint, UpdateSourceTrigger=PropertyChanged}" 
                                     Margin="5" ToolTip="e.g., https://yourcluster.kusto.windows.net"/>

                            <TextBlock Grid.Row="1" Text="Database:" VerticalAlignment="Center" Margin="5"/>
                            <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding KustoDatabase, UpdateSourceTrigger=PropertyChanged}" Margin="5"/>
                        </Grid>
                    </GroupBox>

                    <GroupBox Grid.Row="1" Header="Query Templates" Margin="0,10,0,0">
                        <StackPanel Margin="5">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <ComboBox ItemsSource="{Binding KustoTemplates}" SelectedItem="{Binding SelectedKustoTemplate}"
                                          DisplayMemberPath="Name" Margin="0,0,5,5"/>
                                <Button Grid.Column="1" Content="📥 Load" Command="{Binding LoadKustoTemplateCommand}"
                                        Width="70" ToolTip="Load template to query box"/>
                            </Grid>
                            <TextBlock Text="{Binding SelectedKustoTemplate.Description}" Foreground="Gray" 
                                       TextWrapping="Wrap" Margin="0,0,0,5"/>
                        </StackPanel>
                    </GroupBox>

                    <GroupBox Grid.Row="2" Header="Kusto Query" Margin="0,10,0,0">
                        <TextBox Text="{Binding KustoQuery, UpdateSourceTrigger=PropertyChanged}"
                                 AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto"
                                 FontFamily="Consolas" FontSize="12"/>
                    </GroupBox>

                    <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,10,0,0">
                        <Button Content="▶️ Execute Query" Command="{Binding RunKustoQueryCommand}"
                                Height="35" Width="150" Background="#00BCD4" Foreground="White"
                                IsEnabled="{Binding IsRunning, Converter={StaticResource InverseBoolConverter}}"/>
                        <TextBlock Text="💡 Copy query to Azure Data Explorer for execution" 
                                   VerticalAlignment="Center" Margin="15,0,0,0" Foreground="Gray"/>
                    </StackPanel>
                </Grid>
            </TabItem>

            <!-- Local KQL Database Tab -->
            <TabItem Header="🗄️ Local KQL">
                <Grid Margin="10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="250"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Left Panel - Database &amp; Templates -->
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <!-- Database Status -->
                        <GroupBox Header="📊 Local Database">
                            <StackPanel Margin="5">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="Status:" VerticalAlignment="Center" Margin="0,0,10,0"/>
                                    <Border Grid.Column="1" CornerRadius="3" Padding="5,2">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Setter Property="Background" Value="#555"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding LocalDbStatus}" Value="Connected">
                                                        <Setter Property="Background" Value="#4CAF50"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding LocalDbStatus}" Value="Error">
                                                        <Setter Property="Background" Value="#F44336"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <TextBlock Text="{Binding LocalDbStatus}" Foreground="White" FontWeight="Bold"/>
                                    </Border>
                                </Grid>
                                <TextBlock Text="{Binding TotalRecords, StringFormat='Total Records: {0:N0}'}" Margin="0,5,0,0"/>
                                <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                                    <Button Content="🔌 Initialize" Command="{Binding InitializeLocalDbCommand}" 
                                            Width="80" Height="28" Margin="0,0,5,0"
                                            IsEnabled="{Binding IsRunning, Converter={StaticResource InverseBoolConverter}}"/>
                                    <Button Content="🔄 Refresh" Command="{Binding RefreshTablesCommand}" 
                                            Width="70" Height="28" Margin="0,0,5,0"/>
                                    <Button Content="🗑️ Clear" Command="{Binding ClearLocalDbCommand}" 
                                            Width="60" Height="28" Background="#F44336" Foreground="White"/>
                                </StackPanel>
                            </StackPanel>
                        </GroupBox>

                        <!-- Tables Browser -->
                        <GroupBox Grid.Row="1" Header="📁 Tables" Margin="0,10,0,0">
                            <StackPanel Margin="5">
                                <ListBox ItemsSource="{Binding AvailableTables}" 
                                         SelectedItem="{Binding SelectedTable}" 
                                         MaxHeight="100" Margin="0,0,0,5"/>
                                <Button Content="📝 Insert Table" Command="{Binding InsertTableQueryCommand}"
                                        Height="25" IsEnabled="{Binding SelectedTable, Converter={StaticResource StringToBoolConverter}}"/>
                                <TextBlock Text="Columns:" FontWeight="Bold" Margin="0,10,0,5"/>
                                <ListBox ItemsSource="{Binding TableColumns}" MaxHeight="80" FontSize="11"/>
                            </StackPanel>
                        </GroupBox>

                        <!-- KQL Templates -->
                        <GroupBox Grid.Row="2" Header="📋 KQL Cheat Sheet" Margin="0,10,0,0">
                            <Grid Margin="5">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>
                                <ComboBox ItemsSource="{Binding LocalKqlCategories}" 
                                          SelectedItem="{Binding SelectedLocalKqlCategory}" 
                                          Margin="0,0,0,5"/>
                                <ListBox Grid.Row="1" ItemsSource="{Binding FilteredLocalKqlTemplates}" 
                                         SelectedItem="{Binding SelectedLocalKqlTemplate}"
                                         ScrollViewer.VerticalScrollBarVisibility="Auto">
                                    <ListBox.ItemTemplate>
                                        <DataTemplate>
                                            <StackPanel Margin="2">
                                                <TextBlock Text="{Binding Name}" FontWeight="SemiBold" FontSize="11"/>
                                                <TextBlock Text="{Binding Description}" Foreground="Gray" 
                                                           FontSize="10" TextWrapping="Wrap" MaxWidth="200"/>
                                            </StackPanel>
                                        </DataTemplate>
                                    </ListBox.ItemTemplate>
                                </ListBox>
                            </Grid>
                        </GroupBox>
                    </Grid>

                    <!-- Right Panel - Query &amp; Results -->
                    <Grid Grid.Column="1" Margin="10,0,0,0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="150"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="80"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <!-- Query Input -->
                        <TextBlock Text="KQL Query:" FontWeight="Bold" Margin="0,0,0,5"/>
                        <TextBox Grid.Row="1" Text="{Binding LocalKqlQuery, UpdateSourceTrigger=PropertyChanged}"
                                 AcceptsReturn="True" TextWrapping="Wrap" 
                                 VerticalScrollBarVisibility="Auto"
                                 FontFamily="Consolas" FontSize="12"/>

                        <!-- Execution Buttons -->
                        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,10,0,10">
                            <Button Content="▶️ Execute KQL" Command="{Binding ExecuteLocalKqlCommand}"
                                    Height="35" Width="130" Background="#4CAF50" Foreground="White" FontWeight="Bold"
                                    IsEnabled="{Binding IsRunning, Converter={StaticResource InverseBoolConverter}}"/>
                            <Button Content="📤 Export Results" Command="{Binding ExportLocalResultsCommand}"
                                    Height="35" Width="120" Margin="10,0,0,0"/>
                        </StackPanel>

                        <!-- Translated SQL -->
                        <TextBlock Grid.Row="3" Text="Translated SQL:" FontWeight="Bold" Margin="0,0,0,5"/>
                        <TextBox Grid.Row="3" Text="{Binding TranslatedSql, Mode=OneWay}" 
                                 IsReadOnly="True" Margin="0,20,0,0"
                                 TextWrapping="Wrap" VerticalScrollBarVisibility="Auto"
                                 FontFamily="Consolas" FontSize="11" Background="#1E1E1E" Foreground="#9CDCFE"/>

                        <!-- Results Grid -->
                        <TextBlock Grid.Row="4" Text="Results:" FontWeight="Bold" Margin="0,10,0,5"/>
                        <DataGrid Grid.Row="5" ItemsSource="{Binding LocalKqlResults}" 
                                  AutoGenerateColumns="True" IsReadOnly="True" CanUserAddRows="False"
                                  CanUserSortColumns="True" EnableRowVirtualization="True"
                                  VirtualizingPanel.IsVirtualizing="True"
                                  VirtualizingPanel.VirtualizationMode="Recycling"
                                  FontFamily="Consolas" FontSize="11"/>
                    </Grid>
                </Grid>
            </TabItem>

            <!-- Malware Analysis Tab -->
            <TabItem Header="🦠 Malware">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="10">
                        
                        <GroupBox Header="🔧 oletools (Office Macro Analysis)" Margin="0,0,0,10">
                            <StackPanel Margin="5">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="120"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>

                                    <TextBlock Text="oletools Path:" VerticalAlignment="Center" Margin="5"/>
                                    <TextBox Grid.Column="1" Text="{Binding OletoolsPath, UpdateSourceTrigger=PropertyChanged}" Margin="5"/>
                                    <Button Grid.Column="2" Content="Browse" Command="{Binding BrowseOletoolsCommand}" Width="80" Margin="5"/>

                                    <TextBlock Grid.Row="1" Text="Documents:" VerticalAlignment="Center" Margin="5"/>
                                    <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding DocumentPath, UpdateSourceTrigger=PropertyChanged}" Margin="5"/>
                                    <Button Grid.Row="1" Grid.Column="2" Content="Browse" Command="{Binding BrowseDocumentsCommand}" Width="80" Margin="5"/>
                                </Grid>

                                <WrapPanel Margin="5,10,5,5">
                                    <CheckBox Content="olevba (VBA extraction)" IsChecked="{Binding RunOlevba}" Margin="10,5"/>
                                    <CheckBox Content="mraptor (Macro Raptor)" IsChecked="{Binding RunMraptor}" Margin="10,5"/>
                                </WrapPanel>
                            </StackPanel>
                        </GroupBox>

                        <GroupBox Header="📄 PDF Analysis" Margin="0,0,0,10">
                            <StackPanel Margin="5">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="120"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <TextBlock Text="pdf-parser.py:" VerticalAlignment="Center" Margin="5"/>
                                    <TextBox Grid.Column="1" Text="{Binding PdfParserPath, UpdateSourceTrigger=PropertyChanged}" Margin="5"/>
                                    <Button Grid.Column="2" Content="Browse" Command="{Binding BrowsePdfParserCommand}" Width="80" Margin="5"/>
                                </Grid>

                                <CheckBox Content="Analyze PDFs for JavaScript/embedded objects" IsChecked="{Binding RunPdfParser}" Margin="10,10,5,5"/>
                            </StackPanel>
                        </GroupBox>

                        <Button Content="🦠 Analyze Documents for Malware" Command="{Binding RunMalwareAnalysisCommand}"
                                Height="40" FontSize="14" FontWeight="Bold" 
                                Background="#F44336" Foreground="White"
                                IsEnabled="{Binding IsRunning, Converter={StaticResource InverseBoolConverter}}"/>

                        <GroupBox Header="Analysis Results" Margin="0,10,0,0">
                            <DataGrid ItemsSource="{Binding MalwareResults}" AutoGenerateColumns="False"
                                      IsReadOnly="True" CanUserAddRows="False" MaxHeight="200">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="File" Binding="{Binding FileName}" Width="200"/>
                                    <DataGridTextColumn Header="Type" Binding="{Binding FileType}" Width="100"/>
                                    <DataGridCheckBoxColumn Header="Macros" Binding="{Binding HasMacros}" Width="60"/>
                                    <DataGridCheckBoxColumn Header="Suspicious" Binding="{Binding IsSuspicious}" Width="70"/>
                                </DataGrid.Columns>
                            </DataGrid>
                        </GroupBox>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>

            <!-- Bulk Extractor Tab -->
            <TabItem Header="🔎 Extract">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="10">
                        <GroupBox Header="bulk_extractor - Feature Extraction" Margin="0,0,0,10">
                            <StackPanel Margin="5">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="140"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>

                                    <TextBlock Text="bulk_extractor Path:" VerticalAlignment="Center" Margin="5"/>
                                    <TextBox Grid.Column="1" Text="{Binding BulkExtractorPath, UpdateSourceTrigger=PropertyChanged}" Margin="5"/>
                                    <Button Grid.Column="2" Content="Browse" Command="{Binding BrowseBulkExtractorCommand}" Width="80" Margin="5"/>

                                    <TextBlock Grid.Row="1" Text="Input (folder/image):" VerticalAlignment="Center" Margin="5"/>
                                    <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding BulkExtractorInput, UpdateSourceTrigger=PropertyChanged}" Margin="5"/>
                                    <Button Grid.Row="1" Grid.Column="2" Content="Browse" Command="{Binding BrowseBulkInputCommand}" Width="80" Margin="5"/>
                                </Grid>
                            </StackPanel>
                        </GroupBox>

                        <GroupBox Header="Features to Extract" Margin="0,0,0,10">
                            <WrapPanel Margin="5">
                                <CheckBox Content="Email Addresses" IsChecked="{Binding ExtractEmails}" Margin="10,5"/>
                                <CheckBox Content="URLs" IsChecked="{Binding ExtractUrls}" Margin="10,5"/>
                                <CheckBox Content="IP Addresses" IsChecked="{Binding ExtractIpAddresses}" Margin="10,5"/>
                                <CheckBox Content="Credit Card Numbers" IsChecked="{Binding ExtractCreditCards}" Margin="10,5"/>
                            </WrapPanel>
                        </GroupBox>

                        <Button Content="🔎 Run bulk_extractor" Command="{Binding RunBulkExtractorCommand}"
                                Height="40" FontSize="14" FontWeight="Bold" 
                                Background="#607D8B" Foreground="White"
                                IsEnabled="{Binding IsRunning, Converter={StaticResource InverseBoolConverter}}"/>

                        <GroupBox Header="Extracted Features" Margin="0,10,0,0">
                            <DataGrid ItemsSource="{Binding BulkExtractorResults}" AutoGenerateColumns="False"
                                      IsReadOnly="True" CanUserAddRows="False" MaxHeight="250">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="Type" Binding="{Binding Type}" Width="100"/>
                                    <DataGridTextColumn Header="Value" Binding="{Binding Value}" Width="*"/>
                                    <DataGridTextColumn Header="Source" Binding="{Binding Source}" Width="200"/>
                                </DataGrid.Columns>
                            </DataGrid>
                        </GroupBox>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>

            <!-- OpenSearch Integration Tab -->
            <TabItem Header="🔎 OpenSearch">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="10">
                        <GroupBox Header="OpenSearch Connection" Margin="0,0,0,10">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="120"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <TextBlock Text="Server URL:" VerticalAlignment="Center" Margin="5"/>
                                <TextBox Grid.Column="1" Text="{Binding OpenSearchUrl, UpdateSourceTrigger=PropertyChanged}" Margin="5"/>
                                <Button Grid.Column="2" Content="Test" Command="{Binding TestOpenSearchCommand}" Width="80" Margin="5"/>

                                <TextBlock Grid.Row="1" Text="Index Name:" VerticalAlignment="Center" Margin="5"/>
                                <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding IndexName, UpdateSourceTrigger=PropertyChanged}" Margin="5"/>
                                <TextBlock Grid.Row="1" Grid.Column="2" VerticalAlignment="Center" Margin="10,0"
                                           Text="{Binding OpenSearchConnectionStatus}"/>

                                <TextBlock Grid.Row="2" Text="Pipeline:" VerticalAlignment="Center" Margin="5"/>
                                <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding PipelineName, UpdateSourceTrigger=PropertyChanged}" Margin="5"/>
                            </Grid>
                        </GroupBox>

                        <GroupBox Header="Ingest Pipelines" Margin="0,0,0,10">
                            <StackPanel Margin="5">
                                <TextBlock Text="Create normalization pipelines for artifact types:" Margin="0,0,0,10"/>
                                <WrapPanel>
                                    <Border BorderBrush="Gray" BorderThickness="1" CornerRadius="3" Padding="5" Margin="5">
                                        <TextBlock Text="dfir-volatility"/>
                                    </Border>
                                    <Border BorderBrush="Gray" BorderThickness="1" CornerRadius="3" Padding="5" Margin="5">
                                        <TextBlock Text="dfir-evtx"/>
                                    </Border>
                                    <Border BorderBrush="Gray" BorderThickness="1" CornerRadius="3" Padding="5" Margin="5">
                                        <TextBlock Text="dfir-amcache"/>
                                    </Border>
                                    <Border BorderBrush="Gray" BorderThickness="1" CornerRadius="3" Padding="5" Margin="5">
                                        <TextBlock Text="dfir-mftecmd"/>
                                    </Border>
                                    <Border BorderBrush="Gray" BorderThickness="1" CornerRadius="3" Padding="5" Margin="5">
                                        <TextBlock Text="osint-docmeta"/>
                                    </Border>
                                </WrapPanel>
                                <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                                    <Button Content="🔧 Create Pipelines" Command="{Binding CreatePipelinesCommand}"
                                            Height="35" Width="150" Margin="0,0,10,0"/>
                                    <Button Content="📋 Create Index Templates" Command="{Binding CreateIndexTemplatesCommand}"
                                            Height="35" Width="180"/>
                                </StackPanel>
                            </StackPanel>
                        </GroupBox>

                        <Button Content="⬆️ Ingest Artifacts to OpenSearch" Command="{Binding IngestToOpenSearchCommand}"
                                Height="40" FontSize="14" FontWeight="Bold" 
                                Background="#FF9800" Foreground="White"
                                IsEnabled="{Binding IsRunning, Converter={StaticResource InverseBoolConverter}}"/>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>

            <!-- OSINT/Metadata Tab -->
            <TabItem Header="📄 OSINT">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <Grid Margin="10">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <GroupBox Header="Document Metadata Extraction (FOCA-style)" Margin="0,0,0,10">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="120"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <TextBlock Text="Documents Path:" VerticalAlignment="Center" Margin="5"/>
                                <TextBox Grid.Column="1" Text="{Binding DocumentPath, UpdateSourceTrigger=PropertyChanged}" Margin="5"/>
                                <Button Grid.Column="2" Content="Browse" Command="{Binding BrowseDocumentsCommand}" Width="80" Margin="5"/>

                                <TextBlock Grid.Row="1" Text="ExifTool Path:" VerticalAlignment="Center" Margin="5"/>
                                <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding ExifToolPath, UpdateSourceTrigger=PropertyChanged}" Margin="5"/>
                                <Button Grid.Row="1" Grid.Column="2" Content="Browse" Command="{Binding BrowseExifToolCommand}" Width="80" Margin="5"/>
                            </Grid>
                        </GroupBox>

                        <Button Grid.Row="1" Content="🔍 Extract Metadata" Command="{Binding ExtractMetadataCommand}"
                                Height="40" FontSize="14" FontWeight="Bold" 
                                Background="#9C27B0" Foreground="White" Margin="0,0,0,10"
                                IsEnabled="{Binding IsRunning, Converter={StaticResource InverseBoolConverter}}"/>

                        <GroupBox Grid.Row="2" Header="Extracted Metadata">
                            <DataGrid ItemsSource="{Binding ExtractedMetadata}" AutoGenerateColumns="False"
                                      IsReadOnly="True" CanUserAddRows="False">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="File" Binding="{Binding FileName}" Width="200"/>
                                    <DataGridTextColumn Header="Author" Binding="{Binding Author}" Width="150"/>
                                    <DataGridTextColumn Header="Company" Binding="{Binding Company}" Width="150"/>
                                    <DataGridTextColumn Header="Creator" Binding="{Binding Creator}" Width="120"/>
                                    <DataGridTextColumn Header="Software" Binding="{Binding Software}" Width="120"/>
                                    <DataGridTextColumn Header="Modified" Binding="{Binding Modified, StringFormat=yyyy-MM-dd HH:mm}" Width="120"/>
                                </DataGrid.Columns>
                            </DataGrid>
                        </GroupBox>
                    </Grid>
                </ScrollViewer>
            </TabItem>

            <!-- Results Tab -->
            <TabItem Header="📊 Results">
                <Grid Margin="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="5"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- Artifacts List -->
                    <GroupBox Header="Collected Artifacts">
                        <DataGrid ItemsSource="{Binding Artifacts}" AutoGenerateColumns="False"
                                  IsReadOnly="True" CanUserAddRows="False">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Type" Binding="{Binding Type}" Width="120"/>
                                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="150"/>
                                <DataGridTextColumn Header="Source" Binding="{Binding Source}" Width="200"/>
                                <DataGridTextColumn Header="Records" Binding="{Binding RecordCount}" Width="80"/>
                                <DataGridTextColumn Header="Time" Binding="{Binding Timestamp, StringFormat=HH:mm:ss}" Width="80"/>
                                <DataGridTextColumn Header="Output Path" Binding="{Binding OutputPath}" Width="*"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </GroupBox>

                    <GridSplitter Grid.Row="1" Height="5" HorizontalAlignment="Stretch"/>

                    <!-- Log Output -->
                    <GroupBox Grid.Row="2" Header="Activity Log">
                        <TextBox Text="{Binding OutputLog, Mode=OneWay}" 
                                 IsReadOnly="True" TextWrapping="Wrap" 
                                 VerticalScrollBarVisibility="Auto"
                                 FontFamily="Consolas" FontSize="11"/>
                    </GroupBox>
                </Grid>
            </TabItem>
        </TabControl>

        <!-- Footer -->
        <Grid Grid.Row="2" Margin="0,10,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Orientation="Horizontal">
                <TextBlock Text="{Binding StatusMessage}" VerticalAlignment="Center" Margin="0,0,20,0"/>
                <ProgressBar Width="200" Height="20" Value="{Binding Progress}" Maximum="100"
                             Visibility="{Binding IsRunning, Converter={StaticResource BooleanToVisibilityConverter}}"/>
            </StackPanel>

            <Button Grid.Column="1" Content="📤 Export" Command="{Binding ExportResultsCommand}" Width="90" Margin="5" Padding="6,2"/>
            <Button Grid.Column="2" Content="🗑️ Clear" Command="{Binding ClearLogCommand}" Width="90" Margin="5" Padding="6,2"/>
            <Button Grid.Column="3" Content="❌ Cancel" Command="{Binding CancelCommand}" Width="90" Margin="5" Padding="6,2"
                    Background="#f44336" Foreground="White"
                    Visibility="{Binding IsRunning, Converter={StaticResource BooleanToVisibilityConverter}}"/>
        </Grid>
    </Grid>
</UserControl>
