<UserControl x:Class="PlatypusTools.UI.Views.UpscalerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:PlatypusTools.UI.ViewModels"
             mc:Ignorable="d" d:DesignHeight="400" d:DesignWidth="600">
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" MinHeight="100" />
            <RowDefinition Height="5" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="150" MinHeight="80" />
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,8">
            <TextBlock VerticalAlignment="Center" Margin="0,0,8,0">Output folder:</TextBlock>
            <TextBox Width="400" Text="{Binding OutputFolder, UpdateSourceTrigger=PropertyChanged}" />
            <Button Content="Browse..." Command="{Binding BrowseOutputCommand}" Width="90" Margin="8,0,0,0" />
        </StackPanel>
        <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="0,0,0,8">
            <Button Content="Add Files..." Command="{Binding AddFilesCommand}" Width="120" />
            <Button Content="Upscale" Command="{Binding UpscaleCommand}" Width="120" Margin="8,0,0,0" />
            <Button Content="Cancel" Command="{Binding CancelCommand}" Width="90" Margin="8,0,0,0" />
            <TextBlock VerticalAlignment="Center" Margin="8,0,0,0">Scale:</TextBlock>
            <ComboBox ItemsSource="{Binding ScaleOptions}" SelectedItem="{Binding SelectedScale}" Width="60" Margin="4,0,0,0" />
        </StackPanel>
        <GroupBox Header="Files to Upscale" Grid.Row="2" Margin="0,0,0,8">
            <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
                <DataGrid ItemsSource="{Binding Files}" AutoGenerateColumns="False"
                          CanUserAddRows="False" CanUserDeleteRows="False"
                          CanUserResizeColumns="True" CanUserSortColumns="True"
                          IsReadOnly="True"
                          ColumnHeaderHeight="32"
                          HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Disabled">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="File Name" Binding="{Binding}" Width="*" MinWidth="200" CanUserResize="True"/>
                    </DataGrid.Columns>
                </DataGrid>
            </ScrollViewer>
        </GroupBox>

        <!-- GridSplitter -->
        <GridSplitter Grid.Row="3" Height="5" HorizontalAlignment="Stretch" 
                      Background="#CCCCCC" Cursor="SizeNS" ResizeDirection="Rows"/>

        <ProgressBar Grid.Row="4" Height="16" IsIndeterminate="{Binding IsRunning}" Margin="0,8,0,0" />
        <TextBox Grid.Row="5" Text="{Binding Log}" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" TextWrapping="Wrap" Margin="0,8,0,0" IsReadOnly="True" />
    </Grid>
</UserControl>