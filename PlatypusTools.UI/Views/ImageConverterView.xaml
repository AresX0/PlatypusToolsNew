<UserControl x:Class="PlatypusTools.UI.Views.ImageConverterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:PlatypusTools.UI.ViewModels"
             mc:Ignorable="d" d:DesignHeight="400" d:DesignWidth="600">
    <Grid Margin="8">
        <!-- ...existing code... -->
        <!-- ...existing code... -->
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="150" />
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,8">
            <TextBlock VerticalAlignment="Center" Margin="0,0,8,0">Output folder:</TextBlock>
            <TextBox Width="400" Text="{Binding OutputFolder, UpdateSourceTrigger=PropertyChanged}" />
            <Button Content="Browse..." Command="{Binding BrowseOutputCommand}" MinWidth="180" Margin="8,0,0,0" />
        </StackPanel>
        <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="0,0,0,8">
            <Button Content="Add Files..." Command="{Binding AddFilesCommand}" MinWidth="180" />
            <Button Content="Convert" Command="{Binding ConvertCommand}" MinWidth="180" Margin="8,0,0,0" />
            <Button Content="Cancel" Command="{Binding CancelCommand}" MinWidth="180" Margin="8,0,0,0" />
            <Button Content="Clear" Command="{Binding ClearFilesCommand}" MinWidth="180" Margin="8,0,0,0" />
            <Separator Margin="8,0"/>
            <TextBlock VerticalAlignment="Center" Margin="0,0,4,0">Format:</TextBlock>
            <ComboBox Width="70" ItemsSource="{Binding OutputFormats}" SelectedItem="{Binding SelectedOutputFormat}" Margin="0,0,8,0"/>
            <TextBlock VerticalAlignment="Center" Margin="8,0,0,0" Visibility="{Binding IsSvgFormat, Converter={StaticResource BoolToVisibilityConverter}}">SVG Mode:</TextBlock>
            <ComboBox Width="120" ItemsSource="{Binding SvgModes}" SelectedItem="{Binding SelectedSvgMode}" Margin="4,0,0,0" Visibility="{Binding IsSvgFormat, Converter={StaticResource BoolToVisibilityConverter}}"/>
            <TextBlock VerticalAlignment="Center" Margin="8,0,0,0">JPG Quality:</TextBlock>
            <TextBox Width="60" Text="{Binding JpegQuality, UpdateSourceTrigger=PropertyChanged}" Margin="4,0,0,0" />
            <TextBlock VerticalAlignment="Center" Margin="8,0,0,0">Max W:</TextBlock>
            <TextBox Width="60" Text="{Binding MaxWidth, UpdateSourceTrigger=PropertyChanged}" Margin="4,0,0,0" />
            <TextBlock VerticalAlignment="Center" Margin="8,0,0,0">Max H:</TextBlock>
            <TextBox Width="60" Text="{Binding MaxHeight, UpdateSourceTrigger=PropertyChanged}" Margin="4,0,0,0" />
        </StackPanel>
        <ListBox Grid.Row="2" ItemsSource="{Binding Files}" />
        <ProgressBar Grid.Row="3" Height="16" Value="{Binding Progress}" Minimum="0" Maximum="100" Margin="0,8,0,0" />
        <TextBox Grid.Row="4" Text="{Binding Log}" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" TextWrapping="Wrap" Margin="0,8,0,0" IsReadOnly="True" />
    </Grid>
</UserControl>