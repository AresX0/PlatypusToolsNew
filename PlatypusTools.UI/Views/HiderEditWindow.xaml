<Window x:Class="PlatypusTools.UI.Views.HiderEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Edit Hider Entry" Height="220" Width="420" WindowStartupLocation="CenterOwner">
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,8">
            <TextBlock Width="120" VerticalAlignment="Center">Folder:</TextBlock>
            <TextBox Width="200" Text="{Binding FolderPath, UpdateSourceTrigger=PropertyChanged}" />
            <Button Content="Browse..." MinWidth="180" Margin="8,0,0,0" Click="Browse_Click" />
        </StackPanel>

        <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="0,0,0,8">
            <TextBlock Width="120" VerticalAlignment="Center">Password:</TextBlock>
            <PasswordBox Width="250" x:Name="PwdBox" x:FieldModifier="public" />
        </StackPanel>

        <StackPanel Orientation="Horizontal" Grid.Row="2" Margin="0,0,0,8">
            <CheckBox IsChecked="{Binding AclRestricted}" Content="Restrict to Administrators" />
        </StackPanel>

        <StackPanel Orientation="Horizontal" Grid.Row="3" Margin="0,0,0,8">
            <CheckBox IsChecked="{Binding EfsEnabled}" Content="Use EFS" />
        </StackPanel>

        <StackPanel Orientation="Horizontal" Grid.Row="4" HorizontalAlignment="Right">
            <Button Content="OK" MinWidth="180" Margin="0,0,8,0" Click="Ok_Click" />
            <Button Content="Cancel" MinWidth="180" Click="Cancel_Click" />
        </StackPanel>
    </Grid>
</Window>