<UserControl x:Class="PlatypusTools.UI.Views.AudioLibraryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:PlatypusTools.UI.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="700" d:DesignWidth="1200"
             Background="{DynamicResource BackgroundBrush}">
    
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="220"/>
            <ColumnDefinition Width="4"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Sidebar -->
        <Border Grid.Column="0" Background="{DynamicResource SecondaryBackgroundBrush}" BorderThickness="0,0,1,0" BorderBrush="{DynamicResource BorderBrush}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <!-- Library Header -->
                <StackPanel Grid.Row="0" Margin="15,15,15,10">
                    <TextBlock Text="🎵 Library" FontSize="20" FontWeight="Bold" Foreground="{DynamicResource PrimaryTextBrush}"/>
                    <TextBlock FontSize="12" Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,4,0,0">
                        <Run Text="{Binding TrackCount, Mode=OneWay}"/>
                        <Run Text=" tracks • "/>
                        <Run Text="{Binding TotalDuration, Mode=OneWay}"/>
                    </TextBlock>
                </StackPanel>
                <!-- Navigation Menu -->
                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="10,0">
                        <TextBlock Text="BROWSE" FontSize="11" FontWeight="SemiBold" Foreground="{DynamicResource SecondaryTextBrush}" Margin="5,10,0,5"/>
                        <Button Content="📋 All Tracks" Command="{Binding RefreshCommand}" HorizontalContentAlignment="Left" Padding="8,6" Margin="0,2" Background="Transparent" BorderThickness="0"/>
                        <Button Content="👤 Artists" Command="{Binding ShowArtistsCommand}" HorizontalContentAlignment="Left" Padding="8,6" Margin="0,2" Background="Transparent" BorderThickness="0"/>
                        <Button Content="💾 Albums" Command="{Binding ShowAlbumsCommand}" HorizontalContentAlignment="Left" Padding="8,6" Margin="0,2" Background="Transparent" BorderThickness="0"/>
                        <Button Content="🎭 Genres" Command="{Binding ShowGenresCommand}" HorizontalContentAlignment="Left" Padding="8,6" Margin="0,2" Background="Transparent" BorderThickness="0"/>
                        <TextBlock Text="SMART PLAYLISTS" FontSize="11" FontWeight="SemiBold" Foreground="{DynamicResource SecondaryTextBrush}" Margin="5,15,0,5"/>
                        <Button Content="🕐 Recently Played" Command="{Binding ShowRecentlyPlayedCommand}" HorizontalContentAlignment="Left" Padding="8,6" Margin="0,2" Background="Transparent" BorderThickness="0"/>
                        <Button Content="🔥 Most Played" Command="{Binding ShowMostPlayedCommand}" HorizontalContentAlignment="Left" Padding="8,6" Margin="0,2" Background="Transparent" BorderThickness="0"/>
                        <Button Content="⭐ Top Rated" Command="{Binding ShowTopRatedCommand}" HorizontalContentAlignment="Left" Padding="8,6" Margin="0,2" Background="Transparent" BorderThickness="0"/>
                        <!-- User Playlists Section -->
                        <Grid Margin="5,15,0,5">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="PLAYLISTS" FontSize="11" FontWeight="SemiBold" Foreground="{DynamicResource SecondaryTextBrush}"/>
                            <Button Grid.Column="1" Content="➕" Command="{Binding CreatePlaylistCommand}" Width="24" Height="24" Background="Transparent" BorderThickness="0" ToolTip="Create Playlist"/>
                        </Grid>
                        <ItemsControl ItemsSource="{Binding Playlists}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Button Content="{Binding Name}" Command="{Binding DataContext.SelectPlaylistCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" HorizontalContentAlignment="Left" Padding="8,6" Margin="0,2" Background="Transparent" BorderThickness="0"/>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </ScrollViewer>
                <!-- Watch Folders -->
                <Expander Grid.Row="2" Header="Watch Folders" IsExpanded="False" Margin="10,5">
                    <StackPanel>
                        <ListBox ItemsSource="{Binding WatchFolders}" SelectedItem="{Binding SelectedWatchFolder}" MaxHeight="100" Background="Transparent" BorderThickness="0">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding}" TextTrimming="CharacterEllipsis" FontSize="11"/>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                        <StackPanel Orientation="Horizontal" Margin="0,5">
                            <Button Content="Add" Command="{Binding AddFolderCommand}" Padding="10,3" Margin="0,0,5,0"/>
                            <Button Content="Remove" Command="{Binding RemoveFolderCommand}" Padding="10,3"/>
                        </StackPanel>
                    </StackPanel>
                </Expander>
                <!-- Scan Button -->
                <StackPanel Grid.Row="3" Margin="10,5,10,15">
                    <Button Command="{Binding ScanFolderCommand}" IsEnabled="{Binding IsScanning, Converter={StaticResource InverseBoolConverter}}" Padding="10,8">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="📂" FontSize="16" Margin="0,0,8,0"/>
                            <TextBlock Text="Scan Folder" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                    <ProgressBar Value="{Binding ScanProgress}" Maximum="100" Height="3" Margin="0,5,0,0" Visibility="{Binding IsScanning, Converter={StaticResource BoolToVisibilityConverter}}"/>
                </StackPanel>
            </Grid>
        </Border>
        <!-- Splitter -->
        <GridSplitter Grid.Column="1" Width="4" HorizontalAlignment="Center" VerticalAlignment="Stretch" Background="{DynamicResource BorderBrush}"/>
        <!-- Main Content Area -->
        <Grid Grid.Column="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <!-- Search and Actions Bar -->
            <Border Grid.Row="0" Background="{DynamicResource CardBackgroundBrush}" Padding="15,10">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" MaxWidth="400"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <!-- Search Box -->
                    <TextBox Grid.Column="0" Text="{Binding SearchQuery, UpdateSourceTrigger=PropertyChanged}" Padding="30,8,8,8" VerticalContentAlignment="Center">
                        <TextBox.Style>
                            <Style TargetType="TextBox">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="TextBox">
                                            <Border Background="{DynamicResource SecondaryBackgroundBrush}" CornerRadius="4" BorderThickness="1" BorderBrush="{DynamicResource BorderBrush}">
                                                <Grid>
                                                    <TextBlock Text="🔍" VerticalAlignment="Center" Margin="8,0,0,0" Foreground="{DynamicResource SecondaryTextBrush}"/>
                                                    <ScrollViewer x:Name="PART_ContentHost" Margin="28,0,0,0"/>
                                                    <TextBlock Text="Search library..." VerticalAlignment="Center" Margin="28,0,0,0" Foreground="{DynamicResource SecondaryTextBrush}" Visibility="{Binding Text.Length, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource ZeroToVisibleConverter}}"/>
                                                </Grid>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </TextBox.Style>
                    </TextBox>
                    <!-- Actions -->
                    <StackPanel Grid.Column="2" Orientation="Horizontal">
                        <Button Content="▶️ Play All" Command="{Binding PlayAllCommand}" Padding="12,6" Margin="5,0"/>
                        <Button Content="🔀 Shuffle" Command="{Binding ShufflePlayCommand}" Padding="12,6" Margin="5,0"/>
                    </StackPanel>
                </Grid>
            </Border>
            <!-- Track List -->
            <DataGrid x:Name="TrackDataGrid" Grid.Row="1"
                      ItemsSource="{Binding Tracks}"
                      SelectedItem="{Binding SelectedTrack}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      SelectionMode="Extended"
                      ColumnHeaderHeight="32"
                      CanUserResizeColumns="True"
                      CanUserSortColumns="True"
                      MinHeight="250"
                      HorizontalScrollBarVisibility="Auto"
                      VerticalScrollBarVisibility="Auto">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="#" Binding="{Binding TrackNumber}" Width="50" MinWidth="40" IsReadOnly="True" CanUserResize="True" />
                    <DataGridTextColumn Header="Title" Binding="{Binding DisplayTitle}" Width="250" MinWidth="150" IsReadOnly="True" CanUserResize="True">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="FontWeight" Value="Bold" />
                                <Setter Property="Foreground" Value="DarkBlue" />
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                    <DataGridTextColumn Header="Artist" Binding="{Binding DisplayArtist}" Width="180" MinWidth="120" IsReadOnly="True" CanUserResize="True" />
                    <DataGridTextColumn Header="Album" Binding="{Binding DisplayAlbum}" Width="180" MinWidth="120" IsReadOnly="True" CanUserResize="True" />
                    <DataGridTextColumn Header="Genre" Binding="{Binding Genre}" Width="120" MinWidth="80" IsReadOnly="True" CanUserResize="True" />
                    <DataGridTextColumn Header="Duration" Binding="{Binding DurationFormatted}" Width="80" MinWidth="70" IsReadOnly="True" CanUserResize="True" />
                    <DataGridTextColumn Header="Plays" Binding="{Binding PlayCount}" Width="70" MinWidth="50" IsReadOnly="True" CanUserResize="True">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="FontSize" Value="10" />
                                <Setter Property="Foreground" Value="Gray" />
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                </DataGrid.Columns>
                <DataGrid.RowStyle>
                    <Style TargetType="DataGridRow">
                        <EventSetter Event="MouseDoubleClick" Handler="TrackRow_DoubleClick"/>
                    </Style>
                </DataGrid.RowStyle>
                <DataGrid.ContextMenu>
                    <ContextMenu>
                        <MenuItem Header="▶️ Play" Command="{Binding PlayTrackCommand}" CommandParameter="{Binding SelectedTrack}"/>
                        <MenuItem Header="➕ Add to Queue" Command="{Binding AddToQueueCommand}" CommandParameter="{Binding SelectedTrack}"/>
                        <Separator/>
                        <MenuItem Header="Add to Playlist">
                            <MenuItem Header="New Playlist..." Command="{Binding CreatePlaylistCommand}"/>
                        </MenuItem>
                    </ContextMenu>
                </DataGrid.ContextMenu>
            </DataGrid>
            <!-- Empty State -->
            <StackPanel Grid.Row="1" VerticalAlignment="Center" HorizontalAlignment="Center" Visibility="{Binding Tracks.Count, Converter={StaticResource ZeroToVisibleConverter}}">
                <TextBlock Text="🎵" FontSize="48" HorizontalAlignment="Center" Opacity="0.5"/>
                <TextBlock Text="No tracks in library" FontSize="18" Foreground="{DynamicResource SecondaryTextBrush}" HorizontalAlignment="Center" Margin="0,10,0,5"/>
                <TextBlock Text="Scan a folder to add music to your library" FontSize="13" Foreground="{DynamicResource TertiaryTextBrush}" HorizontalAlignment="Center"/>
                <Button Content="📂 Scan Folder" Command="{Binding ScanFolderCommand}" HorizontalAlignment="Center" Padding="20,10" Margin="0,20,0,0"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
