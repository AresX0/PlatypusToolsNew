<UserControl x:Class="PlatypusTools.UI.Views.FileDiffView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignHeight="700" d:DesignWidth="1200">
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="5" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- File Selection -->
        <GroupBox Header="File Comparison" Grid.Row="0" Margin="0,0,0,4">
            <Grid Margin="8">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <WrapPanel Grid.Row="0" Margin="0,0,0,8">
                    <TextBlock Text="Left File:" VerticalAlignment="Center" Width="70" />
                    <TextBox Text="{Binding LeftFilePath, UpdateSourceTrigger=PropertyChanged}" Width="400" Margin="0,0,8,0" />
                    <Button Content="Browse..." Command="{Binding BrowseLeftCommand}" MinWidth="80" Margin="0,0,8,0" />
                </WrapPanel>

                <WrapPanel Grid.Row="1" Margin="0,0,0,8">
                    <TextBlock Text="Right File:" VerticalAlignment="Center" Width="70" />
                    <TextBox Text="{Binding RightFilePath, UpdateSourceTrigger=PropertyChanged}" Width="400" Margin="0,0,8,0" />
                    <Button Content="Browse..." Command="{Binding BrowseRightCommand}" MinWidth="80" Margin="0,0,8,0" />
                </WrapPanel>

                <WrapPanel Grid.Row="2">
                    <Button Content="Compare" Command="{Binding CompareCommand}" MinWidth="100" FontWeight="Bold"
                            Background="#FF4CAF50" Foreground="White" Margin="0,0,8,0" />
                    <Button Content="Swap Files" Command="{Binding SwapCommand}" MinWidth="80"
                            Background="#FF2196F3" Foreground="White" Margin="0,0,8,0" />
                    <Button Content="Copy Diff" Command="{Binding CopyDiffCommand}" MinWidth="80"
                            Background="#FF9C27B0" Foreground="White" Margin="0,0,16,0" />
                    <TextBlock VerticalAlignment="Center" Margin="8,0,0,0">
                        <Run Text="Added: " /><Run Text="{Binding AddedCount, Mode=OneWay}" Foreground="Green" FontWeight="Bold" />
                        <Run Text="  Removed: " /><Run Text="{Binding RemovedCount, Mode=OneWay}" Foreground="Red" FontWeight="Bold" />
                        <Run Text="  Unchanged: " /><Run Text="{Binding UnchangedCount, Mode=OneWay}" Foreground="{DynamicResource SecondaryTextBrush}" />
                    </TextBlock>
                </WrapPanel>
            </Grid>
        </GroupBox>

        <GridSplitter Grid.Row="1" Height="5" HorizontalAlignment="Stretch"
                      Background="#CCCCCC" Cursor="SizeNS" ResizeDirection="Rows" />

        <!-- Diff Results -->
        <GroupBox Header="Diff Results" Grid.Row="2">
            <DataGrid ItemsSource="{Binding DiffLines}" AutoGenerateColumns="False"
                      IsReadOnly="True" CanUserSortColumns="False"
                      HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto"
                      GridLinesVisibility="Horizontal" AlternatingRowBackground="{DynamicResource ControlBackgroundBrush}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Line" Binding="{Binding LineNumber}" Width="60" />
                    <DataGridTemplateColumn Header="Type" Width="80">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Type}" FontWeight="Bold"
                                           HorizontalAlignment="Center" Padding="4,2">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Type}" Value="Added">
                                                    <Setter Property="Foreground" Value="Green" />
                                                    <Setter Property="Background" Value="#E8F5E9" />
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Type}" Value="Removed">
                                                    <Setter Property="Foreground" Value="Red" />
                                                    <Setter Property="Background" Value="#FFEBEE" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTextColumn Header="Content" Binding="{Binding Content}" Width="*">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="FontFamily" Value="Consolas" />
                                <Setter Property="TextTrimming" Value="CharacterEllipsis" />
                                <Setter Property="ToolTip" Value="{Binding Content}" />
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                </DataGrid.Columns>
                <DataGrid.RowStyle>
                    <Style TargetType="DataGridRow">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding Type}" Value="Added">
                                <Setter Property="Background" Value="#E8F5E9" />
                            </DataTrigger>
                            <DataTrigger Binding="{Binding Type}" Value="Removed">
                                <Setter Property="Background" Value="#FFEBEE" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.RowStyle>
            </DataGrid>
        </GroupBox>

        <!-- Status Bar -->
        <StatusBar Grid.Row="3">
            <StatusBarItem>
                <TextBlock Text="{Binding StatusMessage}" />
            </StatusBarItem>
        </StatusBar>
    </Grid>
</UserControl>
