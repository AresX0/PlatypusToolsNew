<Window x:Class="PlatypusTools.UI.Views.PromptPasswordWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Enter password" Height="160" Width="420" WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TextBlock x:Name="MessageText" Text="Enter password:" Grid.Row="0" Margin="0,0,0,8" />
        <PasswordBox x:Name="PwdBox" Grid.Row="1" Width="360" x:FieldModifier="public" />
        <StackPanel Orientation="Horizontal" Grid.Row="2" HorizontalAlignment="Right" Margin="0,8,0,0">
            <Button Content="OK" MinWidth="180" Margin="0,0,8,0" Click="Ok_Click" />
            <Button Content="Cancel" MinWidth="180" Click="Cancel_Click" />
        </StackPanel>
    </Grid>
</Window>