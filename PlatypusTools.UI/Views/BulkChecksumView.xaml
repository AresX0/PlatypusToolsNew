<UserControl x:Class="PlatypusTools.UI.Views.BulkChecksumView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignHeight="700" d:DesignWidth="1200">
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="5" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Text="🔢 Bulk Checksum Verifier" FontSize="16" FontWeight="Bold" Margin="0,0,0,8" />

        <!-- Controls -->
        <GroupBox Grid.Row="1" Header="Settings" Margin="0,0,0,4">
            <WrapPanel Margin="8">
                <TextBlock Text="Algorithm:" VerticalAlignment="Center" Margin="0,0,4,0" />
                <ComboBox ItemsSource="{Binding Algorithms}" SelectedItem="{Binding SelectedAlgorithm}" Width="90" Margin="0,0,12,0" />
                <Button Content="📂 Add Files" Command="{Binding AddFilesCommand}" Padding="10,4" Margin="0,0,4,0"
                        Background="#FF2196F3" Foreground="White" />
                <Button Content="📁 Add Folder" Command="{Binding AddFolderCommand}" Padding="10,4" Margin="0,0,4,0"
                        Background="#FF2196F3" Foreground="White" />
                <Button Content="▶ Compute" Command="{Binding ComputeCommand}" Padding="12,4" Margin="0,0,4,0"
                        Background="#FF4CAF50" Foreground="White" />
                <Button Content="📋 Load Checksum File" Command="{Binding LoadChecksumFileCommand}" Padding="10,4" Margin="0,0,4,0"
                        Background="#FFFF9800" Foreground="White" />
                <Button Content="💾 Export" Command="{Binding ExportCommand}" Padding="8,4" Margin="0,0,4,0"
                        Background="#FF9C27B0" Foreground="White" />
                <Button Content="🗑️ Clear" Command="{Binding ClearCommand}" Padding="8,4" Margin="0,0,4,0" />
                <Button Content="⛔ Cancel" Command="{Binding CancelCommand}" Padding="8,4"
                        Background="#FFF44336" Foreground="White" />
            </WrapPanel>
        </GroupBox>

        <GridSplitter Grid.Row="2" Height="5" HorizontalAlignment="Stretch"
                      Background="#CCCCCC" Cursor="SizeNS" ResizeDirection="Rows"/>

        <!-- Results Tab Control -->
        <TabControl Grid.Row="3" SelectedIndex="{Binding SelectedTabIndex}">
            <!-- Compute Results -->
            <TabItem Header="📊 Checksums">
                <DataGrid ItemsSource="{Binding Results}"
                          AutoGenerateColumns="False" IsReadOnly="True" SelectionMode="Single"
                          GridLinesVisibility="Horizontal" AlternatingRowBackground="#FF2A2A3A"
                          Background="#FF1E1E2E" Foreground="White" BorderThickness="0">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="File" Binding="{Binding FileName}" Width="200">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="TextTrimming" Value="CharacterEllipsis" />
                                    <Setter Property="ToolTip" Value="{Binding FilePath}" />
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                        <DataGridTextColumn Header="Size" Binding="{Binding FileSizeDisplay}" Width="80" />
                        <DataGridTextColumn Header="Hash" Binding="{Binding Hash}" Width="*" MinWidth="200">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="TextTrimming" Value="CharacterEllipsis" />
                                    <Setter Property="ToolTip" Value="{Binding Hash}" />
                                    <Setter Property="FontFamily" Value="Consolas" />
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                        <DataGridTemplateColumn Header="Actions" Width="80">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Content="📋 Copy" Padding="6,2"
                                            Command="{Binding DataContext.CopyHashCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                            CommandParameter="{Binding}"
                                            Background="#FF2196F3" Foreground="White" />
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>

            <!-- Verification Results -->
            <TabItem Header="✅ Verification">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <!-- Quick verify -->
                    <GroupBox Header="Quick Verify" Margin="0,4">
                        <WrapPanel Margin="8">
                            <TextBlock Text="File:" VerticalAlignment="Center" Margin="0,0,4,0" />
                            <TextBox Text="{Binding VerifyFilePath, UpdateSourceTrigger=PropertyChanged}" Width="250" Margin="0,0,8,0" />
                            <TextBlock Text="Expected Hash:" VerticalAlignment="Center" Margin="0,0,4,0" />
                            <TextBox Text="{Binding ExpectedHash, UpdateSourceTrigger=PropertyChanged}" Width="300" Margin="0,0,8,0"
                                     FontFamily="Consolas" />
                            <TextBlock Text="{Binding DetectedAlgorithm}" VerticalAlignment="Center" Margin="0,0,8,0"
                                       Foreground="#FF4CAF50" />
                            <Button Content="✅ Verify" Command="{Binding VerifyCommand}" Padding="12,4"
                                    Background="#FF4CAF50" Foreground="White" />
                        </WrapPanel>
                    </GroupBox>

                    <DataGrid Grid.Row="1" ItemsSource="{Binding Verifications}"
                              AutoGenerateColumns="False" IsReadOnly="True" SelectionMode="Single"
                              GridLinesVisibility="Horizontal" AlternatingRowBackground="#FF2A2A3A"
                              Background="#FF1E1E2E" Foreground="White" BorderThickness="0">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="" Binding="{Binding StatusIcon}" Width="35" />
                            <DataGridTextColumn Header="File" Binding="{Binding FileName}" Width="200">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="TextTrimming" Value="CharacterEllipsis" />
                                        <Setter Property="ToolTip" Value="{Binding FilePath}" />
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                            <DataGridTextColumn Header="Status" Binding="{Binding StatusText}" Width="100" />
                            <DataGridTextColumn Header="Expected" Binding="{Binding ExpectedHash}" Width="*">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="TextTrimming" Value="CharacterEllipsis" />
                                        <Setter Property="ToolTip" Value="{Binding ExpectedHash}" />
                                        <Setter Property="FontFamily" Value="Consolas" />
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                            <DataGridTextColumn Header="Actual" Binding="{Binding ActualHash}" Width="*">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="TextTrimming" Value="CharacterEllipsis" />
                                        <Setter Property="ToolTip" Value="{Binding ActualHash}" />
                                        <Setter Property="FontFamily" Value="Consolas" />
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>
        </TabControl>

        <!-- Status Bar -->
        <Grid Grid.Row="4" Margin="0,4,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock Text="{Binding StatusMessage}" Foreground="#FFB0B0B0" FontSize="11" VerticalAlignment="Center" />
            <ProgressBar Grid.Column="1" Value="{Binding Progress}" Width="200" Height="14"
                         Visibility="{Binding IsProcessing, Converter={StaticResource BoolToVisibilityConverter}}" />
        </Grid>
    </Grid>
</UserControl>
