<UserControl x:Class="PlatypusTools.UI.Views.ScheduledBackupView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignHeight="700" d:DesignWidth="1200">
    <Grid Margin="8">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="320" MinWidth="260" />
            <ColumnDefinition Width="5" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- Left: Profile Management -->
        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <GroupBox Header="Backup Profiles" Grid.Row="0" Margin="0,0,0,4">
                <StackPanel Margin="4">
                    <ListBox ItemsSource="{Binding Profiles}" SelectedItem="{Binding SelectedProfile}"
                             DisplayMemberPath="Name" Height="100" Margin="0,0,0,8" />
                    <WrapPanel>
                        <Button Content="New" Command="{Binding NewProfileCommand}" MinWidth="60" Margin="0,0,4,0"
                                Background="#FF4CAF50" Foreground="White" />
                        <Button Content="Delete" Command="{Binding DeleteProfileCommand}" MinWidth="60"
                                Background="#FFF44336" Foreground="White" />
                    </WrapPanel>
                </StackPanel>
            </GroupBox>

            <GroupBox Header="Profile Settings" Grid.Row="1">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="4">
                        <TextBlock Text="Profile Name:" Margin="0,0,0,4" />
                        <TextBox Text="{Binding ProfileName, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,8" />

                        <TextBlock Text="Source Folders:" Margin="0,0,0,4" />
                        <ListBox ItemsSource="{Binding SourceFolders}" SelectedItem="{Binding SelectedSourceFolder}"
                                 Height="80" Margin="0,0,0,4" />
                        <WrapPanel Margin="0,0,0,8">
                            <Button Content="Add Folder" Command="{Binding AddSourceFolderCommand}" MinWidth="80" Margin="0,0,4,0"
                                    Background="#FF2196F3" Foreground="White" />
                            <Button Content="Remove" Command="{Binding RemoveSourceFolderCommand}" MinWidth="60"
                                    Background="#FFF44336" Foreground="White" />
                        </WrapPanel>

                        <TextBlock Text="Destination:" Margin="0,0,0,4" />
                        <Grid Margin="0,0,0,8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <TextBox Text="{Binding DestinationFolder, UpdateSourceTrigger=PropertyChanged}" />
                            <Button Content="..." Grid.Column="1" Command="{Binding BrowseDestinationCommand}" Width="30" Margin="4,0,0,0" />
                        </Grid>

                        <WrapPanel Margin="0,0,0,8">
                            <CheckBox Content="Compress (ZIP)" IsChecked="{Binding CompressBackup}" Margin="0,0,16,0" VerticalAlignment="Center" />
                            <CheckBox Content="Incremental" IsChecked="{Binding Incremental}" VerticalAlignment="Center" />
                        </WrapPanel>

                        <WrapPanel Margin="0,0,0,8">
                            <TextBlock Text="Max Versions:" VerticalAlignment="Center" Width="90" />
                            <TextBox Text="{Binding MaxVersions}" Width="50" />
                        </WrapPanel>

                        <TextBlock Text="Exclude Patterns (;-separated):" Margin="0,0,0,4" />
                        <TextBox Text="{Binding ExcludePatterns, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,8"
                                 TextWrapping="Wrap" AcceptsReturn="False" />

                        <Button Content="Save Profile" Command="{Binding SaveProfileCommand}" FontWeight="Bold"
                                Background="#FF4CAF50" Foreground="White" Margin="0,0,0,8" />
                    </StackPanel>
                </ScrollViewer>
            </GroupBox>
        </Grid>

        <GridSplitter Grid.Column="1" Width="5" VerticalAlignment="Stretch"
                      Background="#CCCCCC" Cursor="SizeWE" />

        <!-- Right: Execution & History -->
        <Grid Grid.Column="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <WrapPanel Grid.Row="0" Margin="0,0,0,8">
                <Button Content="Run Backup Now" Command="{Binding RunBackupCommand}" MinWidth="120" FontWeight="Bold"
                        Background="#FF4CAF50" Foreground="White" Margin="0,0,8,0" />
                <Button Content="Cancel" Command="{Binding CancelCommand}" MinWidth="80"
                        Background="#FFF44336" Foreground="White" Margin="0,0,8,0" />
                <Button Content="Refresh History" Command="{Binding RefreshBackupsCommand}" MinWidth="110"
                        Background="#FF2196F3" Foreground="White" Margin="0,0,16,0" />
                <ProgressBar Width="200" Height="20" Value="{Binding Progress}" Maximum="100"
                             Visibility="{Binding IsRunning, Converter={StaticResource BooleanToVisibilityConverter}}" />
            </WrapPanel>

            <!-- Existing backups -->
            <GroupBox Header="Backup History" Grid.Row="1" Margin="0,0,0,4">
                <DataGrid ItemsSource="{Binding ExistingBackups}" AutoGenerateColumns="False"
                          IsReadOnly="True" MaxHeight="150"
                          HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Created" Binding="{Binding Created, StringFormat='yyyy-MM-dd HH:mm'}" Width="140" />
                        <DataGridTextColumn Header="Size" Binding="{Binding Size}" Width="80" />
                        <DataGridTextColumn Header="Path" Binding="{Binding Path}" Width="*">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="TextTrimming" Value="CharacterEllipsis" />
                                    <Setter Property="ToolTip" Value="{Binding Path}" />
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </GroupBox>

            <!-- Log -->
            <GroupBox Header="Log" Grid.Row="2">
                <TextBox Text="{Binding LogOutput}" IsReadOnly="True"
                         TextWrapping="Wrap" AcceptsReturn="True"
                         VerticalScrollBarVisibility="Auto"
                         FontFamily="Consolas" FontSize="11" />
            </GroupBox>

            <StatusBar Grid.Row="3">
                <StatusBarItem>
                    <TextBlock Text="{Binding StatusMessage}" />
                </StatusBarItem>
            </StatusBar>
        </Grid>
    </Grid>
</UserControl>
