<UserControl x:Class="PlatypusTools.UI.Views.SystemHardeningView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignHeight="600" d:DesignWidth="900">
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="5" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Header & Controls -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,8">
            <Button Content="🛡️ Run Security Checks" Command="{Binding RunChecksCommand}"
                    Background="#FF4CAF50" Foreground="White" Padding="16,8" FontWeight="Bold"
                    FontSize="14" Margin="0,0,12,0" />
            <Button Content="📄 Export Report" Command="{Binding ExportReportCommand}"
                    Padding="12,8" Margin="0,0,12,0" />

            <Border Background="#4CAF50" CornerRadius="4" Padding="8,4" Margin="0,0,4,0" VerticalAlignment="Center">
                <TextBlock Foreground="White">
                    <Run Text="✅ " /><Run Text="{Binding PassCount, Mode=OneWay}" />
                </TextBlock>
            </Border>
            <Border Background="#FF9800" CornerRadius="4" Padding="8,4" Margin="0,0,4,0" VerticalAlignment="Center">
                <TextBlock Foreground="White">
                    <Run Text="⚠️ " /><Run Text="{Binding WarnCount, Mode=OneWay}" />
                </TextBlock>
            </Border>
            <Border Background="#F44336" CornerRadius="4" Padding="8,4" VerticalAlignment="Center">
                <TextBlock Foreground="White">
                    <Run Text="❌ " /><Run Text="{Binding FailCount, Mode=OneWay}" />
                </TextBlock>
            </Border>
        </StackPanel>

        <GridSplitter Grid.Row="2" Height="5" HorizontalAlignment="Stretch"
                      Background="#CCCCCC" Cursor="SizeNS" ResizeDirection="Rows" />

        <!-- Results DataGrid -->
        <DataGrid Grid.Row="3" ItemsSource="{Binding Checks}" AutoGenerateColumns="False"
                  IsReadOnly="True" SelectionMode="Single" CanUserAddRows="False"
                  CanUserDeleteRows="False" GridLinesVisibility="Horizontal"
                  HeadersVisibility="Column">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Category" Binding="{Binding Category}" Width="120" />
                <DataGridTextColumn Header="Check" Binding="{Binding Name}" Width="200" />
                <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="180" />
                <DataGridTextColumn Header="Details" Binding="{Binding Details}" Width="*" MinWidth="200">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="TextTrimming" Value="CharacterEllipsis" />
                            <Setter Property="ToolTip" Value="{Binding Details}" />
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="Severity" Binding="{Binding Severity}" Width="80" />
            </DataGrid.Columns>
            <DataGrid.RowStyle>
                <Style TargetType="DataGridRow">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding Severity}" Value="Fail">
                            <Setter Property="Background" Value="#FFFDE7E7" />
                        </DataTrigger>
                        <DataTrigger Binding="{Binding Severity}" Value="Warning">
                            <Setter Property="Background" Value="#FFFFF3E0" />
                        </DataTrigger>
                        <DataTrigger Binding="{Binding Severity}" Value="Pass">
                            <Setter Property="Background" Value="#FFE8F5E9" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.RowStyle>
        </DataGrid>

        <!-- Status Bar -->
        <TextBlock Grid.Row="4" Text="{Binding StatusMessage}" Margin="0,4,0,0" Foreground="Gray" />
    </Grid>
</UserControl>
