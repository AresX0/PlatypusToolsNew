<UserControl x:Class="PlatypusTools.UI.Views.WebsiteDownloaderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="900">
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto" MinHeight="60"/>
            <RowDefinition Height="5"/>
            <RowDefinition Height="*" MinHeight="100"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- URL Input -->
        <GroupBox Header="Website URL" Grid.Row="0" Margin="0,0,0,8">
            <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <Grid Margin="8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox x:Name="UrlTextBox" Grid.Column="0" Text="{Binding Url, UpdateSourceTrigger=PropertyChanged}" 
                         VerticalContentAlignment="Center" Margin="0,0,8,0"/>
                <Button Grid.Column="1" Content="Scan" Width="90" Click="OnScanClick" FontWeight="Bold" Background="#FF2196F3" Foreground="White"/>
            </Grid>
            </ScrollViewer>
        </GroupBox>

        <!-- Download Options -->
        <GroupBox Header="Download Options" Grid.Row="1" Margin="0,0,0,8">
            <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <Grid Margin="8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <CheckBox Grid.Row="0" Grid.Column="0" Content="Download Images" IsChecked="{Binding DownloadImages}" Margin="0,4"/>
                <CheckBox Grid.Row="1" Grid.Column="0" Content="Download Videos" IsChecked="{Binding DownloadVideos}" Margin="0,4"/>
                <CheckBox Grid.Row="2" Grid.Column="0" Content="Download Documents" IsChecked="{Binding DownloadDocuments}" Margin="0,4"/>
                
                <CheckBox Grid.Row="0" Grid.Column="1" Content="Recursive Crawl" IsChecked="{Binding RecursiveCrawl}" Margin="0,4"/>
                <StackPanel Grid.Row="1" Grid.Column="1" Orientation="Horizontal" Margin="0,4">
                    <TextBlock Text="Max Depth:" VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <TextBox Text="{Binding MaxDepth}" Width="50" VerticalContentAlignment="Center"/>
                </StackPanel>
                <StackPanel Grid.Row="2" Grid.Column="1" Orientation="Horizontal" Margin="0,4">
                    <TextBlock Text="URL Pattern:" VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <TextBox Text="{Binding UrlPattern}" Width="150" VerticalContentAlignment="Center"/>
                </StackPanel>
            </Grid>
            </ScrollViewer>
        </GroupBox>

        <!-- Output Directory -->
        <GroupBox Header="Output Directory" Grid.Row="2" Margin="0,0,0,8">
            <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <Grid Margin="8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox x:Name="OutputDirectoryTextBox" Grid.Column="0" Text="{Binding OutputDirectory, UpdateSourceTrigger=PropertyChanged}" 
                         VerticalContentAlignment="Center" Margin="0,0,8,0"/>
                <Button Grid.Column="1" Content="Browse..." Width="90" Click="OnBrowseClick"/>
            </Grid>
            </ScrollViewer>
        </GroupBox>

        <!-- Action Buttons -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,0,0,8">
            <Button Content="Download Selected" Click="OnDownloadClick" Width="130" Margin="0,0,8,0" FontWeight="Bold" Background="#FF4CAF50" Foreground="White"/>
            <Button Content="Cancel" Click="OnCancelClick" Width="80" Margin="0,0,8,0" Background="#FFF44336" Foreground="White"/>
            <Button Content="Select All" Click="OnSelectAllClick" Width="90" Margin="0,0,8,0" Background="#FF2196F3" Foreground="White"/>
            <Button Content="Select None" Click="OnSelectNoneClick" Width="90" Margin="0,0,16,0"/>
            <TextBlock VerticalAlignment="Center" Margin="0,0,4,0">Items:</TextBlock>
            <TextBlock x:Name="ItemCountText" VerticalAlignment="Center" FontWeight="Bold" Text="{Binding DownloadItems.Count, FallbackValue=0}"/>
        </StackPanel>

        <!-- GridSplitter -->
        <GridSplitter Grid.Row="4" Height="5" HorizontalAlignment="Stretch" 
                      Background="#CCCCCC" Cursor="SizeNS" ResizeDirection="Rows"/>

        <!-- Download Items -->
        <GroupBox Header="Download Queue" Grid.Row="5" Margin="0,0,0,8">
            <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
                <DataGrid x:Name="DownloadItemsDataGrid" ItemsSource="{Binding DownloadItems}"
                          AutoGenerateColumns="False" CanUserAddRows="False"
                          CanUserResizeColumns="True" CanUserSortColumns="True"
                          SelectionMode="Single" IsReadOnly="True"
                          ColumnHeaderHeight="32"
                          HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Disabled">
                    <DataGrid.Columns>
                        <DataGridTemplateColumn Header="Select" Width="60" CanUserResize="True" MinWidth="50">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <CheckBox IsChecked="{Binding IsSelected, UpdateSourceTrigger=PropertyChanged}"
                                             HorizontalAlignment="Center"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTextColumn Header="Type" Binding="{Binding Type}" Width="80" MinWidth="60" CanUserResize="True"/>
                        <DataGridTextColumn Header="File Name" Binding="{Binding FileName}" Width="200" MinWidth="150" CanUserResize="True"/>
                        <DataGridTextColumn Header="URL" Binding="{Binding Url}" Width="300" MinWidth="200" CanUserResize="True"/>
                        <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="100" MinWidth="80" CanUserResize="True">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Status}" Value="Completed">
                                            <Setter Property="Foreground" Value="Green"/>
                                            <Setter Property="FontWeight" Value="Bold"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding Status}" Value="Failed">
                                            <Setter Property="Foreground" Value="Red"/>
                                            <Setter Property="FontWeight" Value="Bold"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding Status}" Value="Downloading">
                                            <Setter Property="Foreground" Value="Blue"/>
                                            <Setter Property="FontWeight" Value="Bold"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                        <DataGridTemplateColumn Header="Progress" Width="120" MinWidth="100" CanUserResize="True">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <ProgressBar Value="{Binding Progress}" Height="16" Minimum="0" Maximum="100"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </ScrollViewer>
        </GroupBox>

        <!-- Status Bar -->
        <Grid Grid.Row="6">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="{Binding StatusMessage}" VerticalAlignment="Center"/>
            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <TextBlock Text="Overall:" VerticalAlignment="Center" Margin="0,0,8,0"/>
                <ProgressBar Width="150" Height="20" Value="{Binding Progress}" Minimum="0" Maximum="100"/>
                <TextBlock Text="{Binding Progress, StringFormat={}{0:F0}%}" VerticalAlignment="Center" Margin="8,0,0,0" FontWeight="Bold"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
