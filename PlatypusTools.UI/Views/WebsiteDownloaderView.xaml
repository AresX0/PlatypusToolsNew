<UserControl x:Class="PlatypusTools.UI.Views.WebsiteDownloaderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="900">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- URL Input -->
            <GroupBox Header="Website URL" Grid.Row="0" Margin="0,0,0,10">
                <Grid Margin="5">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBox Grid.Row="0" Grid.Column="0" Text="{Binding Url, UpdateSourceTrigger=PropertyChanged}" 
                             Height="25" VerticalContentAlignment="Center" Margin="0,0,5,0"/>
                    <Button Grid.Row="0" Grid.Column="1" Content="Scan" Width="80" Height="25"
                            Command="{Binding ScanCommand}"/>

                    <TextBlock Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" Margin="0,5,0,0"
                               Text="Example: https://example.com" Foreground="Gray" FontSize="10"/>
                </Grid>
            </GroupBox>

            <!-- Download Options -->
            <GroupBox Header="Download Options" Grid.Row="1" Margin="0,0,0,10">
                <Grid Margin="5">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <CheckBox Grid.Row="0" Grid.Column="0" Content="Download Images" IsChecked="{Binding DownloadImages}" Margin="0,5"/>
                    <CheckBox Grid.Row="1" Grid.Column="0" Content="Download Videos" IsChecked="{Binding DownloadVideos}" Margin="0,5"/>
                    <CheckBox Grid.Row="2" Grid.Column="0" Content="Download Documents" IsChecked="{Binding DownloadDocuments}" Margin="0,5"/>
                    
                    <CheckBox Grid.Row="0" Grid.Column="1" Content="Recursive Crawl" IsChecked="{Binding RecursiveCrawl}" Margin="0,5"/>
                    <StackPanel Grid.Row="1" Grid.Column="1" Orientation="Horizontal" Margin="0,5">
                        <TextBlock Text="Max Depth:" VerticalAlignment="Center" Margin="0,0,10,0"/>
                        <TextBox Text="{Binding MaxDepth}" Width="50" Height="22" VerticalContentAlignment="Center"/>
                    </StackPanel>
                    <StackPanel Grid.Row="2" Grid.Column="1" Orientation="Horizontal" Margin="0,5">
                        <TextBlock Text="URL Pattern:" VerticalAlignment="Center" Margin="0,0,10,0"/>
                        <TextBox Text="{Binding UrlPattern}" Width="150" Height="22" VerticalContentAlignment="Center"/>
                    </StackPanel>
                </Grid>
            </GroupBox>

            <!-- Output Directory -->
            <GroupBox Header="Output Directory" Grid.Row="2" Margin="0,0,0,10">
                <Grid Margin="5">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBox Grid.Column="0" Text="{Binding OutputDirectory, UpdateSourceTrigger=PropertyChanged}" 
                             Height="25" VerticalContentAlignment="Center" Margin="0,0,5,0"/>
                    <Button Grid.Column="1" Content="Browse..." Width="80" Height="25"
                            Command="{Binding BrowseOutputCommand}"/>
                </Grid>
            </GroupBox>

            <!-- Action Buttons -->
            <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,0,0,10">
                <Button Content="Download Selected" Width="130" Height="30" Margin="0,0,5,0"
                        Command="{Binding StartDownloadCommand}"/>
                <Button Content="Cancel" Width="80" Height="30" Margin="0,0,5,0"
                        Command="{Binding CancelCommand}"/>
                <Button Content="Select All" Width="80" Height="30" Margin="0,0,5,0"
                        Command="{Binding SelectAllCommand}"/>
                <Button Content="Select None" Width="80" Height="30" Margin="0,0,5,0"
                        Command="{Binding SelectNoneCommand}"/>
                <Button Content="Clear" Width="80" Height="30" Margin="0,0,5,0"
                        Command="{Binding ClearCommand}"/>
            </StackPanel>

            <!-- Download Items -->
            <DataGrid Grid.Row="4" ItemsSource="{Binding DownloadItems}" 
                      AutoGenerateColumns="False" CanUserAddRows="False" 
                      CanUserResizeColumns="True" CanUserSortColumns="True"
                      SelectionMode="Single" IsReadOnly="True">
                <DataGrid.Columns>
                    <DataGridTemplateColumn Header="Select" Width="60" CanUserResize="False">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <CheckBox IsChecked="{Binding IsSelected, UpdateSourceTrigger=PropertyChanged}" 
                                         HorizontalAlignment="Center"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTextColumn Header="Type" Binding="{Binding Type}" Width="80" MinWidth="60"/>
                    <DataGridTextColumn Header="File Name" Binding="{Binding FileName}" Width="*" MinWidth="150"/>
                    <DataGridTextColumn Header="URL" Binding="{Binding Url}" Width="2*" MinWidth="200"/>
                    <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="100" MinWidth="80"/>
                    <DataGridTemplateColumn Header="Progress" Width="100" MinWidth="80">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <ProgressBar Value="{Binding Progress}" Height="16" Minimum="0" Maximum="100"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>

            <!-- Status Bar -->
            <StatusBar Grid.Row="5" Height="30" Margin="0,10,0,0">
                <StatusBarItem>
                    <TextBlock Text="{Binding StatusMessage}"/>
                </StatusBarItem>
                <StatusBarItem HorizontalAlignment="Right">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Progress: " Margin="0,0,5,0"/>
                        <ProgressBar Width="150" Height="16" Value="{Binding Progress}" 
                                     Minimum="0" Maximum="100"/>
                        <TextBlock Text="{Binding Progress, StringFormat={}{0}%}" Margin="5,0,0,0"/>
                    </StackPanel>
                </StatusBarItem>
            </StatusBar>
        </Grid>
    </ScrollViewer>
</UserControl>
