<UserControl x:Class="PlatypusTools.UI.Views.EmptyFolderScannerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:PlatypusTools.UI.ViewModels">
    <UserControl.DataContext>
        <vm:EmptyFolderScannerViewModel />
    </UserControl.DataContext>
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Folder Selection -->
        <GroupBox Grid.Row="0" Header="Select Folder to Scan" Padding="8" Margin="0,0,0,8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBox Grid.Column="0" Text="{Binding SelectedFolder, UpdateSourceTrigger=PropertyChanged}" 
                         VerticalContentAlignment="Center" Margin="0,0,8,0" />
                <Button Grid.Column="1" Content="Browse..." Command="{Binding BrowseCommand}" 
                        Padding="16,6" Margin="0,0,8,0" />
                <Button Grid.Column="2" Content="Scan" Command="{Binding ScanCommand}" 
                        Padding="24,6" Margin="0,0,8,0" Background="#1E88E5" Foreground="White" />
                <Button Grid.Column="3" Content="Cancel" Command="{Binding CancelCommand}" 
                        Padding="16,6" />
            </Grid>
        </GroupBox>

        <!-- Selection Buttons -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,8">
            <Button Content="Select All" Command="{Binding SelectAllCommand}" Padding="12,4" Margin="0,0,8,0" />
            <Button Content="Select None" Command="{Binding SelectNoneCommand}" Padding="12,4" />
        </StackPanel>

        <!-- Results List -->
        <GroupBox Grid.Row="2" Header="Empty Folders Found" Padding="8">
            <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
                <DataGrid ItemsSource="{Binding EmptyFolders}" AutoGenerateColumns="False"
                          CanUserAddRows="False" CanUserDeleteRows="False"
                          CanUserResizeColumns="True" CanUserSortColumns="True"
                          SelectionMode="Extended"
                          HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Disabled">
                    <DataGrid.Columns>
                        <DataGridCheckBoxColumn Header="Select" Binding="{Binding IsSelected, UpdateSourceTrigger=PropertyChanged}" Width="60" CanUserResize="True" MinWidth="50" />
                        <DataGridTextColumn Header="Folder Name" Binding="{Binding Name}" Width="200" IsReadOnly="True" CanUserResize="True" MinWidth="100" />
                        <DataGridTextColumn Header="Full Path" Binding="{Binding Path}" Width="450" IsReadOnly="True" CanUserResize="True" MinWidth="200" />
                        <DataGridTextColumn Header="Created" Binding="{Binding CreatedDate, StringFormat='{}{0:yyyy-MM-dd HH:mm}'}" Width="140" IsReadOnly="True" CanUserResize="True" MinWidth="100" />
                    </DataGrid.Columns>
                </DataGrid>
            </ScrollViewer>
        </GroupBox>

        <!-- Action Buttons -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
            <Button Content="Delete Selected" Command="{Binding DeleteSelectedCommand}" 
                    Padding="16,8" Margin="0,0,8,0" Background="#F44336" Foreground="White" />
            <Button Content="Delete All" Command="{Binding DeleteAllCommand}" 
                    Padding="16,8" Background="#D32F2F" Foreground="White" />
        </StackPanel>

        <!-- Status Bar -->
        <Border Grid.Row="4" Background="#F5F5F5" Padding="8" Margin="0,8,0,0" CornerRadius="4">
            <TextBlock Text="{Binding StatusMessage}" />
        </Border>
    </Grid>
</UserControl>
