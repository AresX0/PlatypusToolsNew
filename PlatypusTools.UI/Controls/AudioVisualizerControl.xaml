<UserControl x:Class="PlatypusTools.UI.Controls.AudioVisualizerControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="100" d:DesignWidth="600"
             Loaded="OnLoaded"
             Unloaded="OnUnloaded">
    
    <Grid>
        <!-- Visualization Canvas -->
        <Canvas x:Name="VisualizerCanvas" 
                Background="Transparent"
                ClipToBounds="True"/>
        
        <!-- Overlay for mode selection -->
        <ComboBox x:Name="ModeSelector"
                  HorizontalAlignment="Right"
                  VerticalAlignment="Top"
                  Margin="5"
                  Opacity="0.7"
                  Width="100"
                  SelectedIndex="0"
                  SelectionChanged="ModeSelector_SelectionChanged">
            <ComboBoxItem Content="Bars"/>
            <ComboBoxItem Content="Mirror Bars"/>
            <ComboBoxItem Content="Waveform"/>
            <ComboBoxItem Content="Circular"/>
        </ComboBox>
    </Grid>
</UserControl>
