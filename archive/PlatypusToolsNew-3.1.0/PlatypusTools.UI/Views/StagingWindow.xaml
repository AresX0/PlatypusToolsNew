<Window x:Class="PlatypusTools.UI.Views.StagingWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:PlatypusTools.UI.ViewModels"
        mc:Ignorable="d"
        Title="Staging" Height="400" Width="800" WindowStartupLocation="CenterOwner">
    <Window.DataContext>
        <vm:StagingViewModel />
    </Window.DataContext>
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <DataGrid ItemsSource="{Binding StagedFiles}" AutoGenerateColumns="False" CanUserAddRows="False" SelectionMode="Extended"
                  CanUserResizeColumns="True" CanUserSortColumns="True">
            <DataGrid.Columns>
                <DataGridCheckBoxColumn Header="Select" Binding="{Binding IsSelected}" Width="60" CanUserResize="True" MinWidth="40"/>
                <DataGridTextColumn Header="Original Path" Binding="{Binding OriginalPath}" Width="*" CanUserResize="True" MinWidth="150"/>
                <DataGridTextColumn Header="Staged Path" Binding="{Binding StagedPath}" Width="*" CanUserResize="True" MinWidth="150"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
            <Button Content="Open Staging Folder" Command="{Binding OpenFolderCommand}" Width="160" Margin="4" />
            <Button Content="Restore Selected" Command="{Binding RestoreSelectedCommand}" Width="140" Margin="4" />
            <Button Content="Remove Selected" Command="{Binding RemoveSelectedCommand}" Width="140" Margin="4" />
            <Button Content="Commit (Delete Originals)" Command="{Binding CommitSelectedCommand}" Width="200" Margin="4" />
            <Button Content="Close" Click="Close_Click" Width="90" Margin="4" />
        </StackPanel>
    </Grid>
</Window>