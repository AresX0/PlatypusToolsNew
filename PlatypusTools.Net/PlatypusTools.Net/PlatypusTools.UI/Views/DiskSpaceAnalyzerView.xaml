<UserControl x:Class="PlatypusTools.UI.Views.DiskSpaceAnalyzerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignHeight="600" d:DesignWidth="1000">
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Root Path Selection -->
        <GroupBox Header="Disk Space Analysis" Grid.Row="0" Margin="0,0,0,8">
            <Grid Margin="8">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,8">
                    <TextBlock VerticalAlignment="Center" Width="100">Root Path:</TextBlock>
                    <TextBox Width="500" Text="{Binding RootPath, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,8,0" />
                    <Button Content="Browse..." Command="{Binding BrowsePathCommand}" Width="90" Margin="0,0,8,0" />
                    <Button Content="Analyze" Command="{Binding AnalyzeCommand}" Width="90" FontWeight="Bold" Margin="0,0,8,0" />
                    <Button Content="Export" Command="{Binding ExportCommand}" Width="90" />
                </StackPanel>
                <StackPanel Orientation="Horizontal" Grid.Row="1">
                    <TextBlock VerticalAlignment="Center" Width="100">Total Size:</TextBlock>
                    <TextBlock Text="{Binding TotalSizeDisplay}" FontWeight="Bold" FontSize="14" Foreground="DarkBlue" />
                </StackPanel>
            </Grid>
        </GroupBox>

        <!-- Directory Tree -->
        <GroupBox Header="Directory Hierarchy" Grid.Row="1" Margin="0,0,0,8">
            <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
                <TreeView ItemsSource="{Binding DirectoryTree}">
                    <TreeView.ItemContainerStyle>
                        <Style TargetType="TreeViewItem">
                            <Setter Property="IsExpanded" Value="{Binding IsExpanded, Mode=TwoWay}" />
                        </Style>
                    </TreeView.ItemContainerStyle>
                    <TreeView.ItemTemplate>
                        <HierarchicalDataTemplate ItemsSource="{Binding Children}">
                            <StackPanel Orientation="Horizontal">
                                <Image Width="16" Height="16" Margin="0,0,4,0" Source="/Resources/folder.png" />
                                <TextBlock Text="{Binding Name}" FontWeight="Bold" Margin="0,0,8,0" />
                                <TextBlock Text="{Binding SizeDisplay}" Foreground="DarkGreen" Margin="0,0,8,0" />
                                <TextBlock Text="{Binding FileCount, StringFormat='({0} files)'}" Foreground="Gray" />
                            </StackPanel>
                        </HierarchicalDataTemplate>
                    </TreeView.ItemTemplate>
                </TreeView>
            </ScrollViewer>
        </GroupBox>

        <!-- Status Bar -->
        <StatusBar Grid.Row="2">
            <StatusBarItem>
                <TextBlock Text="{Binding StatusMessage}" />
            </StatusBarItem>
            <StatusBarItem HorizontalAlignment="Right">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Analyzing..." Margin="0,0,8,0" Visibility="{Binding IsAnalyzing, Converter={StaticResource BooleanToVisibilityConverter}}" />
                    <ProgressBar Width="100" Height="16" IsIndeterminate="True" Visibility="{Binding IsAnalyzing, Converter={StaticResource BooleanToVisibilityConverter}}" />
                </StackPanel>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</UserControl>
