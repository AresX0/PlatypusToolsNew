# Avalonia Resources

This folder is referenced by the project's `<AvaloniaResource Include="Assets\**" />` glob.

Do **not** copy assets from `PlatypusTools.UI/Assets/` — keep this port self-contained
so changes here never bleed into the Windows tree.

Add cross-platform-friendly icons (SVG / PNG) here as the port grows.
